# SA 53270 - BR Will Call Orders Portal

**Status:** working in P21Play, awaiting Matt's review / UAT. Not yet deployed to Prod.

## File roles

- `br_will_call_orders.retrieve.sql` — source-of-truth query. This is what's pasted into
  InfoMaker's SQL painter; it's also what actually runs in the deployed DataWindow.
- `br_will_call_orders.srd` — **current deployed file**, pulled down from
  `\\ASP21FS1\Play\Portals\br_will_call_orders.srd` 2026-08-22. Built in InfoMaker (the
  PowerBuilder DataWindow painter) by hand, off the retrieve.sql above — not generated by the
  build script below.
- `br_will_call_orders.BROKEN-hand-edited.srd` — **do not deploy.** My first attempt: hand-
  spliced the retrieve SQL + column layout into a text copy of `br_sal_will_advise.BASE.srd`
  via `Build-BrWillCallOrders-Srd.ps1`. Every static check passed (column counts, positional
  order vs. the SQL, no leftover references) but it still crashed P21 on open — PowerBuilder
  couldn't *build* the DataWindow from the file (`SelectElement`/`SelectPage` crash, no SQL
  error surfaced). Kept for the record; see [[feedback_p21_srd_hand_edit_unreliable]].
- `Build-BrWillCallOrders-Srd.ps1` — the script that produced the BROKEN file above.
  **Superseded** — do not use to regenerate the deployed `.srd`. Kept only because its
  verification logic (column-count / positional-order asserts) is a reusable pattern if this
  approach is ever revisited with a fix for whatever InfoMaker's own build does differently.
- `br_sal_will_advise.BASE.srd` / `.retrieve.EXTRACTED.sql` — pristine copy of the live source
  portal (`BR SAL WILL ADVISE`, portal_element_uid 165) pulled from Prod 2026-08-22, and its
  extracted retrieve SQL. Reference only.
- `Register-BrWillCallOrders-Play.sql` — creates the `portal_element` / `portal_user_defined`
  rows in P21Play. Already run (portal_element_uid 325). Idempotent — safe to re-run.
- `Register-BrWillCallOrders-Drill-Play.sql` — creates the `dc_nav_drill` row that makes
  Order # drillable to Order Entry. Already run (dc_nav_drill_uid 152). Idempotent.

## Known loose end

A diagnostic duplicate is still live in P21Play, not yet cleaned up:
- Portal element **"BR WILL CALL ORDERS TEST2"** (portal_element_uid 326)
- File `\\ASP21FS1\Play\Portals\br_will_call_orders_test2.srd`

It was registered mid-session to isolate a stale-cache theory (proved wrong — see the
BROKEN file's note above). Safe to delete once UAT wraps:
```sql
DELETE FROM portal_user_defined WHERE portal_element_uid = 326;
DELETE FROM portal_element      WHERE portal_element_uid = 326;
```
(and delete the file from the Play share).

## Prod promotion (once UAT is approved)

1. Copy `br_will_call_orders.srd` (the InfoMaker-built one, **not** the BROKEN file) to
   `\\ASP21FS1\Prod\Portals\`.
2. Re-run `Register-BrWillCallOrders-Play.sql` and `Register-BrWillCallOrders-Drill-Play.sql`
   against Prod (`P21.allsurfaces.com` / `P21`), with `@lib = '\\ASP21FS1\Prod\Portals'` in the
   first script.
3. Delete the TEST2 diagnostic element/file (Play) if not already done.
