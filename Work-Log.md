# Work Log

**Newest first. One entry per task — not per resume.** When you pick a task back up, update its
existing row; don't add a new one. Move a task to **Completed** once it's accepted/shipped.

**Status key:** 🟡 Awaiting feedback/testing · 🔵 In progress · ⏸ Parked · 🔴 Blocked · 🟢 Done

> The `/resume` GUIDs are a convenience only — transcripts prune at ~30 days, so older ones are dead.
> The durable record is the task, its status, and the memory note linked in **Detail**.

---

## Active — awaiting feedback, in progress, or parked

| Ticket # | Last touched | Task | Status | Where it stands / next step | Detail |
|---|---|---|---|---|---|
| | 2026-08-27 | Warehouse Manager vs. Branch Manager menu-access comparison (no ticket, Mike Learned) | 🟡 Draft prepared, awaiting send + Mike's reply | Compared reachable P21 menus for the two roles in **P21Play** (chosen deliberately — role menus still baselined to Epicor OOB intent, not Prod drift). Result: Warehouse Manager is a **strict superset** of Branch Manager — of 1,185 menu nodes, 276 both / 564 neither / **345 WM-only / 0 BM-only**. Branch Manager is effectively inquiry/reporting only; WM adds inventory/purchasing/production/master-data *maintenance* (WWMS is only 71 of the 345). Learned the view mechanics: `p21_view_dc_menu_security` is deny-based (CROSS JOINs users × `frame_menu`, LEFT JOINs only *disabling* `custom_objects` rows); `version_type='R'` = role-level disable, `'U'` = user-level, `NULL`+`access='Y'` = visible — compared role-level truth only (`version_type='R' AND access='N'`, ignoring `'U'`). Traps: `roles` has only `role_uid`/`role` (WM=20, BM=2); `delete_flag='Y'` users inflate counts (BM 310 rows / 10 active); view is slow (~2 min, CROSS JOIN) — repeat runs should hit `custom_objects`/`custom_objects_detail` (`object_type='M'`, `type='R'`) joined to `roles` directly; `version_type` is `NULL` not `''` when unmatched. Deliverables: Excel `C:\PowerShell-Scripts\Create-Excel\Menu-Access-Diff_WarehouseMgr-vs-BranchMgr_P21Play_20260827-113128.xlsx` (345 rows, full menu path) + Outlook draft to Mike Learned (both tables as HTML, spreadsheet attached) — in **Drafts, not sent**. Read-only, no repo artifact, no `kb_`/`js_` in the chain (stock P21 view). Next: user sends the draft; offered to re-run against Prod to show divergence from the baseline | `reference_p21_menu_security_role_compare.md` |
| | 2026-08-27 | P21 Environment Status — customizations tracker reconciliation + cross-env user audit (no ticket) | 🟡 Tracker reconciled & committed; better team-share format parked pending user research | `Docs\P21-Env-Status-Customizations.txt` was well behind — it's a re-slice of this Work-Log by environment, so entries have to be hand-copied and it had drifted. Reconciled it against the full Work-Log (Active + Completed) + memory: corrected 3 stale statuses (Training `price_page_description` OPEN→DONE; BRR `Test Primary Bin` IN TEST→DONE; BRR order-ack-message ON HOLD→DONE/→SA 53475), added ~40 missing items across Prod / Data Warehouse / P21Dev / P21Play / Training / BRR / Upgrade — biggest omission was the **Sale Price Book/Page Maintenance DynaChanges unique to P21Dev** (Commission App). Built `C:\PowerShell-Scripts\Create-Excel\Find-P21Users-LowerEnvs-NotInProd.ps1` (lower-env users absent from Prod + `delete_flag='N'` with `**` name marker) — logic smoke-tested live: NotInProd = P21Dev 4, Play/Training 1 (NVANWALBECK — present despite the 8/3 "not recreated" row, so re-added manually), BRR 5, Upgrade/Accounting 0; Prod has 1 `**`/not-deleted user. Added a `CROSS-ENV USER AUDIT` section to the tracker. Recommended splitting the doc into a static layer (stays in OneNote) + a row-per-item table (`Environment\|Category\|Item\|Status\|Ticket\|Updated\|Detail`, one filterable Category column) kept as a git CSV and rendered to an Excel workbook on Teams/SharePoint (`.md` isn't team-shareable, `.xlsx` is). User pasted the update into OneNote and will research team-share options. Next: on the user's decision, convert the tracker to CSV + write the generator script + produce the first workbook; run the audit script for its Excel output | `feedback_p21_env_customizations_tracker.md`, `feedback_p21_users_star_marker_convention.md` |
| | 2026-08-26 | Add Class ID 5 as a PORG filter (Purchasing, via Jossy Vadakkel) — no ticket | 🟡 Blocked on Purchasing | Confirmed no native class-based filtering exists anywhere in PO Requirements Generation: `purchase_criteria` has filter columns only for Class ID 4, never referenced by any stored proc (searched every SQL module for `beg_class_id4` — zero hits), and the actual criteria-maintenance DataWindow (`d_purchase_criteria`, pulled directly from the P21 client) has zero exposure to any class_id column at all — only supplier/product-group/location/ABC-class filtering exists. PORG generation itself is entirely stored-proc-driven (`p21_gpor_main_2003` → `_to_table_*`/`_ds` variants), no client-side `ds_` object drives the logic. Traced the user's already-added "Class ID 5" Field Chooser field to its exact cause: a real `purchase_criteria_ud` table (genuine PK/FK/audit trigger) is registered on the screen via Field Chooser, and the specific column (`test_class_id5`, `fc_dataobject_column_uid 464`) has `updateable_flag='N'`/`updatewhereclause_flag='N'` — a fixable but shared (layer-1) config change, not made yet. Checked `inv_mast.class_id5` before recommending any build: only `NULL`/`TEMP` values exist company-wide (Training and Prod both) — no real classification data today. Sent findings to Jossy via Outlook (confirmed sent), asking for concrete use cases and example `po_criteria_id`(s) to test with/without a class value. Next: awaiting Purchasing's reply before any UD-field/Business-Rule build | `project_2026_08_26_porg_class_id5_filter.md` |
| | 2026-08-24 | P21 scheduled-task failure email showing wrong sender ("MG Test" job, no ticket) | 🔵 Diagnosed, fix identified, not yet applied | Traced `Failed : MG Test.msg` (sender "Derek Sievertsen") via a live Extended Events trace on `P21Dev.allsurfaces.com`/`P21BusinessRules` — the job (`SaveWindowJob`/`PoRequirementCustomizationProvider`, targeting `m_generatepurchaseorder`) runs under the shared P21 login `ADMIN`, whose `users.email_address` is stale (`dsievertsen@allsurfaces.com`, set whenever Derek originally configured the env) — that's what P21's scheduler uses as the failure-notification "From" address, confirmed via `p21_sp_send_mail`'s definition and the trace's `username` action. `ADMINISTRATOR` has the same stale address; real `DSIEVERTSEN` user record is `active='N'`. Along the way, ruled out a `UiServerException: Tab page is disabled` red herring (initially misread the trace as targeting `w_scheduled_task_manager` — that was actually the user's own separate live desktop session watching the job run) and confirmed no DynaChange/Version Manager customization exists for the real target window on this env. Fix identified (`UPDATE users SET email_address = '<correct>' WHERE id = 'ADMIN'`) but not run — user confirmed the diagnosis was the answer they needed and ended session there. Not yet checked whether other environments (Prod, Training, Dev, Play) have the same stale address. Next: apply the fix in BusinessRules when ready, then check other envs | `reference_p21_scheduled_job_sender_email.md` |
| | 2026-08-24 | Business Rules SharePoint documentation initiative (2026 Goal 2) | 🔵 In progress, paused for the day | Established a one-page, non-technical Word template (What It Does / When It Runs / Why It Exists / Roles-Users Affected / Owner-Status / Appendix) for `_Veirfied Business Rules`, replacing the old scattered AI-summary docs (nothing deleted yet — new `SAMPLE -` docs sit alongside). Converted all 22 existing rule folders plus 2 newly-found inactive families (`kb_Customer_Closed`, `kb_Order_SaleDay`) = 24 one-pagers drafted, none yet reviewed/approved. Rebuilt `Master List of Business Rules installed in Live.xlsx` as a live-snapshot (not persistent-connection, per user's choice) pull from Prod — 97 active rules, `In Folder`/`Rule Documented` tracking columns added, dates fixed to `mm/dd/yyyy`; old version archived alongside, not deleted. Built a new parallel `Master List of Ribbon Rules installed in Live.xlsx` (14 live rows). Renamed `Business Rules` → `Business Rules Source` to reduce confusion with the verified folder. Real findings flagged in the new docs: `OrderEntryFund_LIVE_V3` is actually inactive despite its name; several rules (`PricePageUpdate`, `kb_Order_RequiredDate_r3`) look like one-time data-fixes left running permanently; `kb_ValidateOO_BlockSave` restricts saves to Saturdays only with no documented reason; `kb_ProperCase`/`kb_ValidateOO_SetCustomerFromOrder` error messages tell users to contact "Karen" (= kbenish, departed). `Class-Identifier (Carrier)` has no matching live row under any name — doc built from code alone, flagged unconfirmed. Found the real Atlas Surcharge Rule is 7 active `apc_`-prefixed objects (not kb_/js_), tied to the still-open Atlas CAS-error blocker — agreed with user this needs its own dedicated pass. 4 other undocumented folders (`DirectShipPriceCheck`, `js_Freight_Charge_not_greater_than_1`, `kb_FrontCounter_CotF_WCShip_WCSigCap`, `kb_Notepad_Entry`) intentionally left undocumented — not found live under any name. Next: Atlas Surcharge Rule deep-dive, then user review of the 24 drafted one-pagers' open ⚠ questions | `project_2026_08_24_br_documentation_sharepoint.md` |
| 53334 | 2026-08-24 | Add ASAP marketing banner image to Order Acknowledgement footer (Matt) | 🟡 Review-request sent to Matt via SysAid, awaiting his testing in Play | Built in **P21Play** on `ACKNOWLEDGEMENTS_user.rpt`. Final placement: Group Footer #1i, directly below the customer notice text — prints once, only on the true last page. Two dead ends along the way, worth remembering: (1) Page Footer + "suppress unless last page" formula broke the live `GenerateAndSendFormJob` email job (`Page Header plus Page Footer is too large for the page`, 40 retries then abort) — Crystal reserves full Page Header/Footer band height on every page regardless of any Suppress formula, suppressing only hides content, doesn't reclaim space; (2) moved to Report Footer (fixed the email error, not part of that fixed per-page budget) but a leftover oversized band from earlier testing pushed the whole section onto its own orphan page — fixed by shrinking the band and relocating into Group Footer #1i instead. Tested clean against a 1-line/1-page order and a 9-line/2-page order (6062152). Source image `\\asp21fs1.ahi.local\Prod\Logos\ASAP_InvoiceAd_2.png` copied to Play's `Logos` folder; pre-edit backup saved to `PlayCrystalBackup\ACKNOWLEDGEMENTS_user - 20260824.rpt`. Play only — not touched in BusinessRules or Prod. Explained to Matt in the review-request why the banner sits *above* the barcode/Document ID row rather than below it (Page Footer is a fixed section pinned to the bottom of every page in Crystal Reports — nothing can render below it). Note sent via SysAid (clipboard); an Outlook draft to Matt exists too but was superseded — left unsent in Drafts, not cleaned up. Next: awaiting Matt's review of placement/sizing/centering, then plan Prod deployment | `Deploy-Guides\SA-53334-order-ack-asap-banner.md` || 53769 | 2026-08-28 | "Directs Auto Buy" / Scheduler setup — Training + Business Rules | 🔴 Blocked — Epicor case finalized awaiting submission; separate IT ticket 53769 filed for a real contributing cause | **8/26 update:** Set Business Rules' Scheduler Location URL (`https://p21businessrules.allsurfaces.com:3444/Scheduler/api`, same pattern as Training's working 8/21 fix), reran P21API Configuration, recycled app pools — this cleared the old `Unknown menu name: m_processscheduledjob` error in Business Rules too (updates the 8/21 note that the URL "wasn't the cause" there — it was, just needed the System Setting fix alongside it). Job now fails further in with a new error: `UiServerException: Tab page is disabled and cannot be selected` in `PoRequirementCustomizationProvider.Process → ChangeData`. Pulled `scheduled_job.job_config` for the test job (`Auto Buy - Test Scheduler`, uid 1707975) directly rather than guessing — bound to `po_criteria_id 466` ("RB - Directs"), only `requirement_type_direct_ship` selected, with a line-level override on the `TABPAGE_DIRECTSHIPS` tab (item `SCHEKSN100GS`, supplier `3012610`, a `required_date` already 9 days stale). User manually ran criteria 466 live — generated a real PO — ruling out a data/demand cause. Live-traced the next run via a one-off Extended Events session (`Sql-Scripts\Extended-Events\Create-XE-AutoBuy-Scheduler-Trace.sql`) and caught the real cause in the act: SQL error 15408 (`"Impersonate Session Security Context" cannot be called in this batch because a simultaneous batch has called it`) firing 3x at the exact failure moment, colliding with a `d_dw_scheduled_task_manager` grid-refresh query from the user's own live `AHI\mgoldyn` session at the identical timestamp — i.e. having Scheduled Task Manager open and watching the job run appears to cause a real SQL-level impersonation-context collision. Trace torn down at session end. **Same day, separate lever — better retest candidate identified:** analyzed `purchase_criteria` joined to `supplier.target_value` across every single-supplier criteria in Training. Of 1,226 suppliers, 1,062 have `target_value IS NULL` and 96 (including **M-D PRO — the exact supplier from the original 8/24 test that never produced a PO**) have `target_value = 0.0000`; only 68 have a genuine positive value. Strongly suggests `$0`/`NULL` is treated by P21 as "Auto-Buy not configured" rather than "trivially cleared" — a plausible explanation for the 8/24 dead end independent of the scheduler/collision issue. Recommended criteria **251** (Jasztex, `$300` target — lowest real positive bar in the table) as the next retest candidate instead of 466/M-D PRO. Not yet executed. Next: close all P21 client windows against Business Rules (especially Scheduled Task Manager) and re-trigger 466 in isolation to confirm/refute the collision theory; separately, pick a positive-target-value criteria (251 recommended) and retest whether Auto-Buy actually produces a PO once Target Value is genuinely satisfied. **8/27 update — retested with criteria 251 in Training, real demand confirmed, still zero POs; then found and ruled out the last remaining lever (Task Type: Auto-Buy); case finalized.** Jami Klitzman built `Test 251` (criteria 251/Jasztex, $300 target) and `Test 08.27.2026` (criteria 786, direct-ship/all suppliers) fresh in Training. Both logged `Success` with real unmet demand (`gpor_run_hdr.detail_count` 21 and 44) and **zero `po_hdr` rows** — checked all of P21Training, most recent PO anywhere in the DB is 8/6/2026 from real users, none from any GPOR/scheduler process. Ruled out target_value definitively (251/Jasztex demand was 2–6x the $300 target and still produced nothing). User found KB0089261 (Relative Dates for Scheduled Task Printing) which pointed at the Schedule ribbon dialog's **Task Type** dropdown — discovered "Auto-Buy" is a distinct option from the default "Save Session" every prior job had used. Built `251 without Auto-Buy` (Task Type Save Session, `scheduled_job_type=1`) alongside a rebuilt `Test 251` (Task Type Auto-Buy, `scheduled_job_type=2`) for a clean side-by-side. Confirmed the distinction 3 independent ways — DB `job_config` diff, live P21-web-client network capture (browser DevTools showing the exact `PUT /data` field write and the DataWindow's own `if(scheduled_job_type=1/2,...)` enablement formulas), and execution (both run multiple times including user-initiated, not just scheduled). **Identical result regardless of Task Type: `Success`, `detail_count=21`, zero POs.** Incidental: `Test 251` got stuck in `running_flag='Y'` for over an hour after a rapid Deactivate→Activate→fire sequence, blocking on-demand retrigger with `JobManagementException: already running` — stop/start cleared it, filed as a secondary question. Live-traced the Training session throughout via a second one-off XE session (`Sql-Scripts\Extended-Events\Create-XE-AutoBuy-Training-Trace.sql`); stopped/dropped at session end, raw `.xel` on the file share plus a full readable export at `C:\_P25\AutoBuy-Training-Trace-2026-08-27.csv`. Every lever we can find has now been tested with real evidence: target value, real demand, the impersonation collision, Save Session vs. Auto-Buy Task Type, scheduled vs. user-initiated triggering, stuck-state. Epicor case finalized at `Docs\Epicor-Case-Directs-Auto-Buy-Scheduled-Job.md` covering all of it plus 4 direct questions for Epicor. All 4 test jobs deactivated, trace cleaned up. Next: **user sign-off on the case text, then submit to Epicor** — this is now blocked on Epicor's response, not further internal testing. **8/28 update — found and ticketed a real, independent contributing-cause candidate before submitting to Epicor.** User asked whether the desktop client having folder access while the API/server doesn't could explain the "Success, zero PO" symptom, since PORG writes output files to `\\asp21fs1.ahi.local\Training\PORG`. Read KB0051397 ("Adding Machine Access for Middleware Server") and KB0047131 (HAR capture, banked for the eventual Epicor case attachment) — KB0051397 confirms this is a known, named P21 pattern: server-side P21 processes reach network shares as the middleware server's own computer account, not the interactive user, and that account needs its own explicit share+NTFS grant. Identified the actual middleware host as **AHI-API1** (confirmed by the user directly on the server, and independently corroborated by an unrelated IIS error log entry showing `IIS APPPOOL\DEV-P21 - P21 SOA-messagehub` running on `AHI-API1`). Proved it empirically: a Scheduled Task on AHI-API1 running as `NT AUTHORITY\SYSTEM` (same network identity the middleware service uses) attempted to write to `\\asp21fs1.ahi.local\Training\PORG` and got **Access Denied**. Checked the existing NTFS ACL on `...\Training\PORG` first — every entry is inherited from a parent with nothing set locally — so scoped the ask to the **`Training` share root** rather than just `\PORG`, reasoning a root-level grant propagates to PORG and any other current/future Training subfolder without repeat tickets; left final scope to IT's judgment. **Filed SysAid ticket 53769** requesting AHI-API1's computer account be granted share+NTFS access to `\\asp21fs1.ahi.local\Training`, referencing KB0051397. Separately flagged (not yet confirmed) a similarly-shaped `path is not valid` error on `\\ASP21FS1.AHI.LOCAL\FileHandler\Import Documents` — different, unrelated share; untested whether it's the same access-denied pattern or a bad/nonexistent path. This doesn't replace the Epicor case (SaveWindowJob still appears to only automate the requirements-compute step regardless of Task Type, which is a separate P21-side question) but is a real, independently fixable contributing cause. Next: awaiting IT to action ticket 53769, then retest Auto-Buy end-to-end in Training once granted; separately test the FileHandler path the same way | `project_2026_08_21_directs_auto_buy_scheduler.md` |
| | 2026-08-21 | Claude Desktop + claude.ai web black-screen → IT use-case doc — personal/work laptop, no ticket | 🟡 Awaiting Tony's read of use-case doc | **Black-screen itself is resolved** — Tony Neuman fixed the local security-proxy/policy block on 8/21; claude.ai and Claude Desktop render normally, confirming the 8/20 diagnosis (proxy 403-ing claude.ai's JS/CSS asset bundles) was the real root cause. Instead of a blanket re-approval, Tony raised a data-security question — what Claude can see on the network — so built `Docs\Claude-Continued-Use-Case.md`/`.docx`: value already delivered (EDI 855 fix, KB/JS retirement, Low Margin Alert, portal cleanup, SSRS perf work), exactly what data Claude can/can't see (no standing access — inherits mgoldyn's own AD/SQL permissions only, sees code/schema/query results but never credentials, non-Prod is the default), flagged confirming AllSurfaces' Anthropic plan tier (Team/Enterprise vs. consumer) and a signed DPA as the real open lever rather than blocking the tool, and proposed guardrails. Not yet sent to Tony — delivery is the user's call. Next: confirm doc was sent and what Tony says | `project_2026_08_19_claude_desktop_black_screen.md`, `project_2026_08_21_claude_use_case_doc.md` |
| | 2026-08-25 | Postman collection for DynaChange Extensibility API — dev tooling, no ticket | ⏸ Paused, design/assignment bodies confirmed but not yet fired successfully | Live-tested `Postman\P21-Dynachanges.postman_collection.json` against BRR and P21Dev. Corrected the `design` and `assignment` POST/PUT bodies from real P21 schema-reference pages (pulled directly from the server, not guessed) after a guessed body threw a server-side `NullReferenceException`; found a real DB-vs-API mismatch (`AssignmentType` is the string `"User"`/`"Role"`, not the SQL table's numeric 1/0). Diagnosed and fixed a Postman gotcha where a stale environment variable was silently shadowing the collection's own token, causing false "Invalid token" errors. Confirmed BRR has zero designs (genuinely empty, not a bug) and P21Dev's middleware is `p21devweb.allsurfaces.com:3448`; P21Dev has real designs so reads should move there while writes (Add/Update/Remove) stay in BRR. GitHub search turned up no existing documentation for this specific API. Next: run Search for Designs against P21Dev for first real data, retry the corrected Add a Design against BRR, then pull `modification`/`full/design` reference pages the same way | `project_2026_08_14_dynachange_postman_setup.md` |
| 52078 | 2026-08-22 | Vendor Invoice Receiving Report redesign — show 810 data instead of PO comparison | 🟡 Copied to P21 Play, awaiting Cindy & Chad's UAT results | Redesign complete and copied into **P21 Play** for user acceptance testing. All known bugs fixed and cross-checked, including a 23-invoice (Training) and 94-invoice (Play) multi-vendor stress test. **Bugs fixed across the full pass:** systemic blank pages (Group Header #1 "New Page Before"/"Keep Together" mismatch vs Play); missing per-invoice Totals line on zero-charge invoices (`inv_hdr_ext_cost`); subreport-frame gap (Suppress formula, corrected running-total name `SumofExtPriceNoFreight1`); **two bugs traced back to the original 2017 stock report, not redesign regressions** — "Invoice Extended Price" double-counting freight (2002-era workaround formula, removed) and a pricing-unit double-conversion bug (5x-low result on non-1:1 pricing units, fixed by using the view's own `extended_price` field instead of re-deriving it); Freight given its own dedicated line (Group Footer #2b, section-level suppress when $0); `sub_AdditionalCharges` subreport's suppress condition reworked to account for freight; double-total eliminated (main Totals line and subreport's internal total now mutually exclusive); redundant hardcoded "Freight" row inside the subreport removed (unconditional suppress); header row vertical spacing fixed; `tracking_no` removed from scope. Version stamp bumped to `2026.2 - 2026-08-21`. Drafted a SysAid ticket update and a UAT-kickoff email to Cindy & Chad (via clipboard for SysAid, not Outlook), noting sample PDFs are attached to the ticket. Next: waiting on Cindy & Chad's UAT feedback in P21 Play before planning Prod deployment — worth flagging to them that 2 of the fixes are real bugs in the *current Prod report*, not just cosmetic redesign items | `project_2026_08_12_sa52078_vendor_invoice_redesign.md`, `project_2026_08_12_vendor_invoice_page_headers.md` |
| | 2026-08-11 | Unknown blank P21 alert (`Untitled.msg`) traced + OE alert-view NULL-token hardening (no ticket) | 🟡 Prod deploy deliberately deferred to after hours | Traced the blank email to order `6073955` (P21Training, 10:29 AM) tripping `uid 102 "Test Verify Alerts"` — the broadest-trigger alert still active on this view. **User independently confirmed the Training-DB source matches**, verifying the trace. Read `p21_sp_alert_generation`'s actual definition for the first time: confirmed `REPLACE()` nulls the whole message section if ANY token registered for the alert's TYPE is NULL, even one the specific alert's own template never references. Deactivated `uid 102` (705) in Play/Training/BusinessRules — stops the noise. Then audited every OE-token column in `p21_view_alert_oe_OrderEntry`, found 6 unwrapped, measured all 6 against 90 days/~1.5M real order lines in all 3 envs (zero actual NULLs — doesn't explain the 6073955 incident itself, which stays open). Hardened anyway as no-cost insurance (proven equivalent on existing data): `Alerts\Low-Margin-Alert\06-harden-oe-view-null-tokens.sql`, deployed to **P21Play/Training/BusinessRules**. **Not yet applied to Prod** — user wants that done after hours, since Prod's copy of this view backs other real, currently-firing alerts. Next: after hours, diff Prod's view against Play's, then decide/deploy the same hardening there | `project_2026_07_13_low_margin_alert.md` |
| | 2026-08-11 | p21web.allsurfaces.com / WWMS SSL cert chain incomplete — `ERR_CERT_AUTHORITY_INVALID` in Chrome only (no ticket) | 🟡 Root cause confirmed by IT — awaiting Aziz to install cert on this device | Cert itself is valid (GoDaddy wildcard `*.allsurfaces.com`, issued 7/14/26, expires 1/28/27). **Aziz Shakoor (IT) confirmed 2026-08-11: root cause is client-side, not server-side** — the issuing/assigning CA changed, and this device's Windows trust store is missing the new intermediate CA cert. Server-side is fine on the web instances. (My initial read — that the server wasn't sending the intermediate — was the wrong side of the same symptom; the exported single-cert file I checked apparently wasn't proof of what the server transmits.) Edge/Firefox unaffected because Firefox preloads known public-CA intermediates itself; Chrome relies on this device's Windows cert store, which doesn't have the new one yet. Confirmed the same symptom on both P21Web and WWMS. Next: Aziz will reach out to install the new intermediate CA cert on this device; once installed, confirm Chrome loads both sites clean | |
| | 2026-08-10 | Flow diagram of business rules firing during P21 order entry — session "BR Flow Diagram" | ⏸ Paused | Analyzed the decompiled `P21.Extensions.dll` SDK (3 folders → 3 docs in `Docs\`). Built two reflection-based PS tools (`Get-BusinessRuleDllStatus-Reflection.ps1`, `Test-BusinessRuleDll.ps1`), tested against live BusinessRules/Prod data, 3 real bugs found and fixed (UNC `LoadFrom` block, `$instance`/`-Instance` variable collision, `Resolve-Path` UNC provider-prefix issue — all documented in script headers). **Confirmed empirically `business_rule_log` does not capture ordinary rule executions** (only ~150 `Invoke` rows since 2018 on Prod, all ribbon-tile/EPF lifecycle noise, not order-entry logic) — a live test order won't help via that table. Found a SQL `LIKE`-vs-`CHARINDEX` false positive while chasing a possible logging toggle (banked as its own lesson). Pivoted to a static config query instead: `business_rule WHERE order_entry_flag='Y' AND row_status_flag=704` gives 30 active rules, all on `w_order_entry_sheet`, with trigger field + save-timing already attached — the real foundation for the diagram, no live test needed. Next: user to confirm whether a "BR logging" toggle exists in the P21 client/admin UI (unknown from SQL alone); then pull source for the ~22 .NET-based rules and actually build the diagram | `project_2026_08_10_br_flow_diagram.md` |
| | 2026-08-10 | P21BusinessRules alert env-tag script — same protection Play/Training already have, BRR didn't | ⏸ Built, not run | `Sql-Scripts\Update-P21BusinessRules-Alert-Message-Env-Tag.sql` written as the BRR sibling of the Play/Training scripts (same PREVIEW-then-UPDATE pattern, idempotent). Uses `P21BRR` as the tag rather than the literal DB name, matching the `Test BRR` shorthand already used for location names. Next: run the PREVIEW block against P21BusinessRules, review, then apply | `Sql-Scripts\Update-P21BusinessRules-Alert-Message-Env-Tag.sql` |
| | 2026-08-07 | kbenish (departed employee) still had live `db_securityadmin`/`db_accessadmin` in Prod + all 4 lower envs — surfaced from a misleading log line | 🟡 Lower envs done, Prod deliberately left open | Investigation of a false-alarm "Fixed: kbenish" log line (a logging bug, since fixed) surfaced a real gap: the 8/4 cleanup only revoked one proc grant, not her full orphaned database-user object (131 grant rows incl. `db_securityadmin`), which was identical in Prod and all 4 lower environments. `DROP USER` executed for `kbenish`/`AHI\kbenish` in P21Play/Training/Dev/BusinessRules, verified clean. Also excluded her from the Phase 3/4 refresh capture queries in all 4 environments so this can't silently recur. **Prod intentionally not touched** — user's call; her SQL login there is disabled (since 1/27/2025), so it's dormant, not active. Next: revisit Prod cleanup if/when wanted | `feedback_kbenish_departed.md` |
| | 2026-08-05 | Inventory access / cycle count suggestion (Tony — "floor is yours" box) | 🟡 Awaiting Tony's reply | Reviewed suggestion that each branch needs its own "ID code" for inventory changes. Confirmed this is a P21 security question — restrict Inventory Adjustments window by user/location, no special code needed. Repetitive cycle counts likely caused by "Add to Cycle Count" checkbox being used on adjustments by multiple people. Drafted Teams reply to Tony; offered to pull who currently has Inventory Adjustment access and raised training as a possible factor. Next: await Tony's reply | |
| | 2026-08-07 | business_rule_log Initialize noise — BusinessRulesDLL share cleanup + dead business_rule config rows (session "BR Log cleanup") | 🔵 In progress, paused for the day | Shifted from filename-guessing to evidence-based: `business_rule` has no column linking `rule_name` to a DLL (confirmed via schema search + the P21 client's own `d_dw_bre_organizer_list.srd` source — its retrieve SQL has no assembly reference either); `rule_name` is a free-text label typed once at Import Rule time, proven via source (`RebatePriceTracker.cs`'s real class is `RebatePriceTracker_Update`). Built `Get-BusinessRuleDllStatus.ps1` (dual-encoding UTF-16+UTF-8, case-insensitive string-literal matching against every `rule_name`, `-Instance` parameterized). **`BusinessRules`: 13 more DLLs archived today** (7 exact-match + 6 source-verified via a local dev source tree) on top of the original 24. **Caught and fixed a real mistake same-day**: `kb_Shipping_IBFSurcharge.dll` was archived on a narrow 2-name check, later found (via Prod) to also embed the Active `kb_RMA_IBFSurcharge_Add` — restored within ~2.5 hrs, zero real usage during the gap, removed from the archive script's list for good. **`Prod`: started for the first time** — 9 files moved (`P21.Accounting.MX.dll` + full `kb_Order_SaleDay*` family), each independently re-verified on Prod's own live data, not assumed from lower envs. `Dev`/`Play`/`Training`/`Upgrade` still only have the original 24-file pass + pending `business_rule` batch delete from 8/5, not yet given the 8/7 evidence-based 2nd pass. Full tracking table (per-rule Assembly Name checks, ~17 rows on BusinessRules still to review one-by-one) + incident writeup: `Deploy-Guides\businessrulesdll-share-cleanup.md`. Next: continue the one-by-one Edit-Business-Rule review on BusinessRules' remaining Inactive rows, decide Prod's `DirectShipPriceCheck`/`OrderEntry_SalesRepAddition` batch, then extend the 2nd-pass method to the other 4 lower envs | `project_2026_08_05_br_log_cleanup.md` |
| 51747 | 2026-08-04 | Printing issue (mhilt) — suspected bad part setup | 🟡 Monitoring — no recurrence yet | Matt provided hard copies of the pick ticket and transfer packing list, both showing missing bin information; reprinting the same documents now shows the bin information correctly. No other users have reported the issue. Log review confirmed numerous SQL syntax errors re: an unclosed quotation mark — does not appear to be a Crystal Reports issue; an extra single quote was somehow introduced into the data stream. **Item Maintenance reviewed items printed 08/03/26 and found the actual culprit: a degree sign (`°`) in the item description for "Schluter Kerdi-Kereck Outside Corner 90° (10)" — corrected.** (Earlier working theory blamed the literal `/` in item_id `SCHKERECK/FI10`; the degree-sign description is now the confirmed source of the bad quote.) Best course of action at this time: monitor for recurrence, nothing further to act on | `project_2026_08_03_sa51747_printing_part_setup.md` |
| | 2026-08-03 | Re-add `NVANWALBECK` contractor user to P21Play after refresh | 🔴 Blocked — not yet recreated | Original SA-51571 build (closed 2026-07-31) only ever existed in Play, never Prod, so today's full-table refresh-from-Prod deleted the row outright. Confirmed missing (zero rows) and confirmed he was active pre-refresh via the morning's Before-script capture CSV: branch 100, role_uid 1 ("All"), `nvanwalbeck@allsurfaces.com`. Neither the After-refresh script nor `Restore-P21Play-AppUsers.ps1` can recreate a fully-deleted row (they only re-enable/restore existing ones). He was originally added manually via the P21 client (not SQL) because `asi_proc_copy_p21_user` has an open bug — same manual path needed again. Next: Add User in P21Play client with the profile above | `project_p21play_refresh.md` |
| 46321 | 2026-08-06 | Update Requested date on Purchase Order Header and Line Items (Jerome Butler) | 🟡 Awaiting user acceptance testing | Code built, fully tested in P21Play (5 real POs), and committed (`52174df`) — see deploy guide for the full test matrix and the two traps found along the way (`IS NULL` guard didn't work since P21 auto-defaults both date columns to order_date on creation; missing `EXECUTE` grant for `p21_application_role`/`PxxiUser` caused silent no-ops). UAT-request note drafted and sent via SysAid to Mike Learned (coordinating), Chad (EDI-driven POs), Pamela (manual PO creation), plus PORG testing and an open ask for any test paths the team knows that weren't covered. Jere signs off once the team's satisfied. Next: await UAT feedback, then deploy to Prod (grant step included in the guide) | `Deploy-Guides\SA-46321-po-required-date.md` |
| 46322 | 2026-07-31 | Inventory Adjustment Cost Derivation with Zero On-Hand and No Site History (Jerome Butler) | 🟡 Jere scheduling a meeting | Follow-up email sent 2026-07-31 asking whether they want to pursue Purchasing-team real-world testing. Jerome replied same day (data had fallen through the cracks); resent the findings + SQL script via SysAid, plus a note that custom automated processes (kb_proc_apply_future_costs/prices, confirmed live nightly jobs) can affect item costs outside the adjustment screen and should be accounted for in testing. Jerome confirmed 2026-07-31 he will schedule a meeting the week of 2026-08-03. Next: check back 2026-08-04 (Tuesday) — if no meeting invite by then, follow up | |
| 49632 | 2026-08-10 | Add SideMarks to pick tickets (Andy Thuston) — pivoted to Plan B | 🟡 Submitted for UAT | **Plan B implementation** (done by user directly, not through a Claude session): header formula reworked to drop SideMark/Job Name logic, output Will Call/COD labels only, renamed `WillCall_COD`. Widened Order Date/Req'd Date/Exp Delivery fields in Page Header c (0.625"→0.900"), adjusted dividers/positions. Added new Page Header d below the order header for PO Number + Job Name (two-column label/data layout), suppressed when both are null/empty. SQL test queries built against `p21_fnt_all_pick_ticket_hdr` covering all 4 scenarios (both null, PO only, Job Name only, Will Call). 5 PDF samples generated and attached to ticket. Submitted to P21 Play for UAT; ticket assigned to Chad to coordinate feedback. Next: await Chad/UAT feedback | |
| 50563 | 2026-07-31 | Ribbon Cutoff times update (Vivian Gray) | 🟡 Awaiting Josy's confirmation | Believed complete — waiting on Josy to confirm before closing. Outlook reminder set for 2026-08-03 (Monday) | |
| | 2026-07-30 | 221 WWMS setup & label testing — Case CS0005507753 (assigned to Jose) | 🔴 Blocked — waiting on Epicor | Logged into P21 WWMS, verified WWMS is enabled. Verified Transfer Receipts default to approved & allocated automatically. Verified no labels assigned to location 221 (initial state). Test-printed WWMS packing list — item description present. Added label definitions: Shipping-Item, Wireless Item Operations-Item, Inventory Movements-Bin, Wireless Shipping-Item. Retested packing list — item description now disappears (core symptom). Ruled out Document Imaging as a factor. Tried unchecking WWMS for the location in the test DB, then tried deleting the WWMS label definition to get back to a clean baseline — blocked by a popup saying the label definition cannot be deleted for WWMS, even though WWMS is unchecked for the location; logged off/on to P21, no change. **Escalated as Case CS0005507753 to Jose** with 3 questions (how to turn off WWMS for a location; how to remove all label definitions after disabling WWMS; how to reprint the WWMS transfer packing list). Next: apply Epicor's answer, then resume disable→remove labels→re-enable→retest plan to isolate what makes the item description disappear | |
| | 2026-07-29 | File organization — SQL/PS scripts spread across C:\Claude, C:\PowerShell-Scripts, OneDrive | ⏸ Parked | Surveyed all three locations; real problem is duplicate-purpose folders with clashing names, not file type. Found a live bug along the way: `C:\Claude\Node\Run-BRR-Verification.ps1`+`build-brr-docx.js` is a stale broken duplicate of the fixed pair in `C:\PowerShell-Scripts\Reports\`. User then asked for a bigger architecture change — root-level `C:\SQL-Scripts` (new) + `C:\PowerShell-Scripts` (existing), sibling to `C:\Claude` rather than nested in it. Three clarifying questions queued (ticket-scoped files stay or move, git-init the new folder or not, flat vs. function-based layout) — session ended before answered. Nothing moved/renamed/deleted yet. Next: re-ask those three questions, then execute | `project_2026_07_29_file_organization.md` |
| | 2026-08-10 | Order Acknowledgment custom email message (no ticket) | 🟡 Awaiting Matt Munson's reply + 🟡 open decision on `t2` | **Design proven end-to-end again after a refresh wiped it.** The 2026-08-10 P21BusinessRules refresh deleted both `asi_email_context_flag` (flag-writer) and `asi_oe_email_close_diag` (OK-button append) — `business_rule` was never captured/restored (see BRR refresh recovery entry above). Manually rebuilt: recreated the `asi_email_context_flag` table + grants, re-registered both rules, confirmed via SQL each is a single clean registration, then proved it live with a controlled test send (marker received in the delivered email). One open question: `asi_oe_email_close_diag` re-registered as Synchronous (`run_type_cd=3424`) instead of the previously-proven Asynchronous (3423) — worked anyway in testing, but differs from the documented known-good config, not fully explained. **New finding:** `asi_oe_order_ack_custom_message_t2` (the FormPreEmail pre-fill fallback, was the only active `_tN` version as of 8/4) is *also* gone post-refresh and was not part of this recovery — its live `CustomMessage` is still placeholder joke text that would pre-fill into every real Order Ack email's memo box, so its absence is arguably fine while still blocked on Matt. Not re-registered; needs a decision. t1 incident closed (apologies sent, still true). Next: decide whether to restore `t2` or leave it out; once Matt replies, build the real (non-diagnostic) rule with his text, register once cleanly in the DB he specifies, retest, promote to Play | `Deploy-Guides\order-ack-custom-email-message.md` |
| 51188 | 2026-07-28 | Receipt Auto-Approve Workflow (WWMS) | 🟡 Awaiting Mike's reply | Root cause confirmed via SQL (no BR/custom code involved — zero rows in `business_rule` for any receiving window in Prod or the WWMS test DB) + P21's own Wireless Warehouse User Guide: WWMS **always** forces PO/transfer/RMA receipts unapproved on save regardless of System Settings; `rf_approve_po_receipts=Y` already on but only enables a separate manual "Save and Approve" action, not a combined one. Extension point identified: `w_inventory_receipt_sheet_rf`. Options (BR on save / new combined action / post-hoc trigger) sent to Mike with no forced recommendation — he wants Play prototyping before committing, and flagged we don't have a proven track record with WWMS RF business rules yet. Timing note added: not a quick fix, won't make the month-end WWMS-at-221 go-live — recommended shipping 221 on standard Save-and-Approve and treating this as a fast-follow. Nothing built or deployed. Next: awaiting Mike's reply, then prototype in Play if he wants to proceed | `project_2026_07_28_sa51188_receipt_autoapprove.md` |
| 51186 | 2026-08-25 | WWMS Putaway Consolidation (Location 221) | 🟡 Primary-bin update run in BusinessRules/Play/Prod (Play+Prod user-reported, not yet independently verified); zone-list fix still awaiting Mike/Jossy | Research only, nothing deployed for the zone-list half — see prior detail below. Root cause confirmed via SQL against P21BusinessRules: warehouse zones exist at 221 (`bin_zone`, 7 rows) but putaway **zone lists** don't (`bin_zone_group`/`bin_zone_x_bin_zone_group`, zero rows) — the actual gap causing "Anywhere." `HITS` putaway-rank scheme exists system-wide but never run for any item at 221; putaway attributes unused entirely. **Proved the fix works end-to-end**: called `p21_putaway_main` directly against 2 throwaway zero-activity items (`ARF13100`/`ARF13106`), captured "before" (`"zone list was NOT found"` → `NOBIN`), built one test zone list scoped to just those 2 items (`ZTEST`, clearly labeled, SQL-sequence UIDs, no counter drift), reran — 463 real candidate bins, trace confirmed the list matched. Also resolved the earlier open question about negative `avail_weight`/bin `MT11A` sorting first: it's an artifact of `max_weight=0` (unlimited) minus current weight, not a real capacity signal — not a concern. Separately confirmed exactly how `Putaway Strategy = Primary Bin/Algorithm` behaves via proc source + a live test: the zone-list step always runs, but an independent primary-bin-availability check runs alongside it and **wins over the zone-list result** if the primary bin is available. Left `ZTEST` test objects in place (user's call) for cross-check via the P21 client's Putaway Trace screen. Drafted (not sent) an email to Mike Learned/Jossy Vadakkel with these findings + the ask on how 221 should be organized. **2026-08-25 — separate lever, same location:** built `Sql-Scripts\Updates\Update-Loc221-Primary-Bins.sql` from Mike Learned's own bin-analysis workbook (`Loc-221-Bins-ML-Update.xlsx`, 8/17, found outside any Claude session) — bulk-assigns `primary_bin` for 5,042 items at 221 (highest-qty bin per item), restricted to items still on `NOBIN`, with 4 pre-flight validation checks. Directly relevant to the finding above: since 221's strategy is already Primary Bin/Algorithm, this is a working consolidation path that doesn't need the zone lists this ticket is still blocked on. **BusinessRules run verified directly via SQL** (`Sql-Scripts\Updates\Verify-Loc221-Primary-Bins.sql`, read-only, run via `Invoke-DbaQuery`): 5,041 of 5,042 rows correctly set, 0 remain `NOBIN`. 1 item (`MAP38439`) has no `inv_mast` match — untouched, never eligible. 23 items (a Schluter Kerdi-product cluster) got their `primary_bin` set correctly but that bin currently shows no `inv_bin` stock for the item — worth a spot-check, not a script defect. **Later same day — Play and Prod:** user ran the script against `P21Play` ("no problems") and then Prod ("commands completed successfully"), both directly in SSMS, not through a Claude-driven query. **Not independently verified** — the verify script has not been run against Play or Prod this session, so exact row counts / exception matches there are unconfirmed. Also drafted a Teams message to Mike reporting the Prod completion (not confirmed sent) and put a SysAid ticket-update note for 51186 on the clipboard for pasting (not confirmed saved). Next: run `Verify-Loc221-Primary-Bins.sql` against Play and Prod to confirm counts; confirm the Teams message + SysAid update landed; continue awaiting Mike/Jossy's reply on zone-list organization | `project_sa51186_putaway_logic.md` |
| | 2026-08-13 | WWMS PO Receiving "Deposit Bin" never auto-populates (no ticket, surfaced while testing SA 51186) | 🔴 Unresolved — Epicor case written, not yet submitted | Separate root cause from SA 51186 above — do not conflate. A blank-Deposit-Bin screenshot (PO 4273958, `GUN131P5`) led to a wrong initial theory (`po_receipts_use_primary_bin='O'`), corrected on closer reading: that setting governs the desktop 856 EDI Receipt approval screen, not the WWMS wireless PO Receiving Deposit screen. Set a real, valid primary bin (`B01A`) on `GUN131P5` — still blank. Ruled out UI-Server caching (recycled `API-P21BusinessRules - P21 SOA-uiserver0` on AHI-API1 — no change, and the SQL was already correct beforehand). Found a leftover zero-qty `inv_bin` row (`F08F`) on the item; deletion correctly blocked by a real FK to `inv_tran_bin_detail` (genuine transaction history), left alone. **Decisive test**: tried `SCHKERECK/FI10` (PO 4319516 line 35), an established item with a real primary bin (`H03C`) holding 35 units — still blank, falsifying both the zero-on-hand and leftover-bin-row theories at once. An Extended Events trace of the full client-server flow for both items showed **zero queries** against `inv_loc`/`primary_bin`/any bin table during the screen load — whatever governs this field isn't hitting the database at all. Checked `location.po_receipts_bin_uid` (blank for 221) and read 8 of the user's saved WWMS reference PDFs — no documented match found for this exact screen/symptom. Wrote up a full case (`Docs\WWMS-PO-Receiving-Deposit-Bin-Epicor-Case.md`, also on clipboard) with both test cases and all ruled-out theories — **not yet submitted to Epicor**. Next: awaiting Mike/Jossy's reply (email drafted, not sent) on whether to consult further or go straight to Epicor | `project_sa51186_putaway_logic.md` |
| 50925 | 2026-07-28 | `CENRBRL1-000003-A150` missing from Inventory Usage 90 Days report (Centaur Floor Systems) | 🟡 Awaiting feedback | Root cause: item's `price_family_id` was `CCS` vs `DEFAULT` on the working comparison item; report filters on `@price_family = ic.price_family_id`, excluding it before sales history was evaluated. Item Maintenance updated `price_family_id` to `DEFAULT` in P21. DW refreshes once daily — fix reflected in tomorrow's run. Done by user directly, not through a Claude session. Next: verify item appears on tomorrow's report | |
| | 2026-07-23 | Remove -WC carriers (scripts run by user, not through a Claude session) | 🟡 Awaiting feedback | Scripts run. Waiting on sign-off from Matt | `Sql-Scripts\Update-WC-Carrier-*.sql` |
| | 2026-07-23 | Loc Demand Pattern column — Location-Supplier Item Order Cycle report (no ticket, Jerome Butler) | 🟡 Awaiting feedback/testing | Added `Loc Demand Pattern` (`p21_view_code_p21.code_description` decoded via `code_no = inv_loc.demand_pattern_cd`) to both Power Query tabs. Saved as `...Current Forecast - Updated.xlsx` in `Documents\Excel` (original untouched); reply drafted in Outlook with file attached, **not sent**. File has not been refreshed with live data yet — needs manual Refresh All on first open. Next: user reviews/sends the draft | `project_2026_07_23_jerome_demand_pattern.md` |
| 50622 | 2026-07-22 | Read Only Global Price Level Name added to Sales Price Book & Page (P21Bus, P21Dev) | ⏸ Parked | Added in both P21Bus and P21Dev. Done by user directly, not through a Claude session | |
| | 2026-08-07 | P21 lower-env location naming drift (Play/Training/BusinessRules/Dev) — surfaced from a suspicion that new Prod locations weren't being communicated | 🟡 Mostly done, 6 rows still open | Confirmed the suspicion: 5 real Prod locations added 7/13–7/29/2026 by `jvadakkel` were never tagged in any lower env. Corrected convention (`<Prod's current name> Test Play`/`Test TRG`/`Test BRR`/`Test Dev`) agreed with user, applied via new reusable `Sync-LocationNames-LowerEnvs.ps1` — 138 renames across all 4 environments, verified via direct SQL. Root cause fixed: all 4 refresh scripts now call the dynamic sync instead of a static hand-typed list that silently missed anything added after it was last typed. **Open:** location `501` is missing entirely from P21BusinessRules; `500/501/520/540/560` missing entirely from P21Dev — not renames, no row exists. Deliberately not raw-INSERTed (`location` has ~150 columns incl. GL/tax linkage). Next: decide whether to build those rows (mirroring Prod) or add via P21 client | `Sync-LocationNames-LowerEnvs.ps1` |
| | 2026-08-12 | **SSRS Reports Performance Improvements** (initiative — like Portal cleanup) | 🟡 Report 1 build verified clean, go-live needs explicit go-ahead | Umbrella program to accelerate slow SSRS reports via the `ASI_ReportCache` columnstore-fact pattern. Read `C:\_P25\SSRS-Report-Performance-Progress.md` on resume. **Report 1 = Five Way** (149,547 ms → 125 ms, ~1,200×, 195 cols `EXCEPT`-equal): built on `asdwdb01`; 2026-07-27 found+fixed a NULL-join bug (memoized UDF mis-labelled `manager_name='None'` on 13,349 rows) + a window-boundary flake + a wrong table name in the refresh proc. **2026-08-12: read `BuildLog` for the 07-20 run** — FACT 3,832,053 rows, `fact-vs-compute`=0/0, `compute-vs-original`=0/0, clean. Log confirms `"NO view swap performed"` — build is proven, but go-live (refresh proc w/ swap, one-time run, post-restore Agent job, grant `SSRS_Users`) touches the live report path and hasn't been executed. Next: get explicit go-ahead, then run go-live steps in the progress file. *(The old SA-50249 ticket was access-only and is closed — this work is separate.)* | `project_ssrs_report_performance.md` / `project_sa50249_five_way_acceleration.md` |
| | 2026-07-15 | YTD Sales ribbon tile — `asi_ribbon_cm_SYTD` (ref 20251229) | 🟡 Ready to test/deploy | Rewrite of `kb_CustRM_getYTDSalesAbbr` is built: critical year-filter bug fixed (VARCHAR `year_for_period` vs INT → blank tile), all kb_ deps removed (now on `p21_sales_history_view`). Never deployed or logged. Next: run test plan TC-01..08 in Dev/Test, then Prod per changelog rollback plan | `Docs\asi_ribbon_cm_SYTD_changelog.txt` |
| | 2026-07-15 | P21 Play API — create orders | 🔵 In progress | Postman auth to Play working; only a GET read exists. Next: hit `/uiserver0/ui/interactive/v1` docs to learn if orders POST-create or must drive UI-server window endpoints | `project_2026_07_15_p21_play_api_orders.md` |
| | 2026-08-03 | Low PAD Margin Alert (duplicate, PAD-scoped) | 🔴 Blocked — rebuilding manually, not firing yet | Duplicates the Team/Purchasing split scoped to `product_group_id = 'PAD'` (original alert excludes PAD), same new MAC/Std-Cost trigger; legacy PAD alert (line_item_profit_percentage trigger) untouched. Built + reviewed in Play (uid 105/106), then deployed to **Prod inactive** (`05-create-pad-alerts-PROD-INACTIVE.sql`, uid 106/107, `row_status_flag=705`, real recipients) — no PAD-equivalent stakeholder sign-off yet, so held inactive same as the original pair. Parity vs. Play confirmed (4/4 alerts now match structurally). Evan Jenkins asked whether PAD margin triggers differ from supplies triggers; drafted reply confirming alerts are in Prod but inactive, key differences: line percentage = **-5** (vs. **+5** for supplies), applies to **PAD product group only**. Reply draft ready to send, not sent. **Post-refresh (same day): the Prod-inactive deploy never made it into Play** — confirmed via `msdb..restorehistory`, the backup used for the refresh predates the 7:49 AM Prod deploy by ~7 hours. Rebuilt the pair via `04-create-pad-alerts-PLAY.sql` (SQL), which never fired despite correct data/config in extensive testing — root cause narrowed to a likely AHI-API1 stale metadata cache (`Low Margin Flag` missing from the client's Column dropdown despite being correctly registered in the DB) but not proven/fixed. Per the user's own past experience that SQL-built alerts are unreliable, deleted the SQL-built pair and is **rebuilding both manually in the P21 client** using `Alerts\Low-Margin-Alert\PAD-Alert-Fields-For-Manual-Rebuild.txt` (exported Subject/Header/Line Items/Footer/trigger fields). Also hit and fixed real P21 counter-drift (`alert_recipient`, `Alert_implementation_query`) from raw-SQL inserts bypassing `p21_set_counter` — see `Sql-Scripts\Check-Fix-Alert-Table-Counters-Play.sql`. Next: confirm manual rebuild fires correctly in Play; separately, send the Evan reply and await sign-off to flip Prod 705→704 | `Deploy-Guides\low-margin-alert.md` |
| | 2026-08-28 | Low Margin Alert (Evan) | 🟡 RSM live, Prod prepped, PAD recipients reconciled — Evan confirmed go-live 9/4, reminder set 9/3 | **Evan signed off on the audience split (7/17); spacing rounds done (7/22, `bc64bef`); Prod still INACTIVE (705) since the 7/31 preservation deploy.** **2026-08-26: forwarded live P21Play samples of both 104 (Team) and 105 (Purchasing MAC) to Evan**, surfacing 3 testing traps (bad test customer via `corp_address_id` exclusion, list price not tripping it, a real-recipient leak caught before send) — see prior entries / `feedback_p21_alerts.md`. **2026-08-27: Evan's reply revealed he'd already listed "RSM" (+ himself, + Jere Butler for Purchasing) as expected recipients.** Built `07-add-rsm-token.sql`: added `contacts.sales_manager_id` self-join + `rsm_email` column to the view (mirrors P21's own stock `p21_view_alert_cr_Opportunities` pattern; deliberately not the deprecated `kb_view_salesrep` path), registered token `rsm_email` (uid 745, `available_areas=80`) on Play. Verified the join resolves correctly via direct SQL (no alert fired) against 6+ real reps. Data quality: 173/188 reps (92%) have a manager, 151 of those (87%) have a usable email. User added `<rsm_email>` in the P21 client (confirmed not yet saved live) and asked to test without emailing a real RSM — found **The Carpet Group Inc (`1108592`)**, whose default rep (Kevin Isken) has no manager at all, letting the blank-RSM edge case be tested risk-free; **user confirmed the alert worked.** Investigated whether P21 logs alert sends for Prod tracking — confirmed it does not (`alert_queued_mail` is a pure transient queue, `email_log` covers document email only, never alerts); user's answer is to **BCC himself**, to be added alongside the RSM recipient. Drafted (not sent) a reply to Evan clarifying the RSM-token approach vs. an inferior "append to salesrep's email" hack floated earlier, disclosing the region-visibility drawback (RSM resolves via manager, not territory) and the send-without-testing limitation, recommending enable-and-monitor; user sent it. **Evan's 3:43pm reply: OK with the approach and the drawback, said "send a test to Tyler Priewe, I'll give him a heads up," and floated turning it on next Wednesday (9/2).** **2026-08-28: found a real, non-contrived test case** — order 6058724 (Carpet Factory Outlet LLC), rep Andrew Restivo, RSM resolves to Tyler Priewe, margin -0.42%/-1.28% (found by reconstructing the view's actual filter/margin logic and running it directly against `oe_hdr`/`oe_line`, since `p21_view_alert_oe_OrderEntry` can't be queried for already-processed orders — it's inner-joined to the transient `pending_alerts` queue). Drafted + user sent a reply to Evan confirming the example and that the real test send to Tyler will carry "P21Play & TEST Email" in the subject so it can't be mistaken for a live Prod alert. **8/28 (continued) — test sent and confirmed working; RSM ported to Prod; PAD recipients reconciled.** Built a fresh qualifying test order recipe against Carpet Factory Outlet LLC/Andrew Restivo→Tyler chain: first attempt (item `CBVSTPMO252034`, matching the original 6058724 line) hit a real inventory block (0 on-hand at location 120, P21 refused to allocate) — swapped to `MAP1785142` @ location 100 (2,480 available), 100 EA @ $16.00 override, re-verified against **current** costs (MAC $15.31/Std $15.84, still -4.5%/-1.0%, both thresholds trip). **User built the order, both 104 and 105 fired — confirmed received.** Ported the RSM change to **Prod**: diffed Prod's live view against Play's first (per standing convention) — confirmed Prod was missing both the 8/11 NULL-hardening (06, separate/still-unapproved, deliberately NOT bundled in) and the RSM column (07); built `07-add-rsm-token-PROD.sql` from Prod's actual current definition (not from the Play script) so only the RSM delta moved, applied via `CREATE OR ALTER VIEW`, token registered (`token_uid 745`, matches Play), join re-verified live against real Prod contacts (Andrew Restivo→Tyler Priewe→`tpriewe@allsurfaces.com`). **User then added RSM as a recipient directly on Prod 104/105/106/107 in the client.** Asked to check whether the PAD pair's (106/107) recipients matched the main pair (104/105) — first checked **Play** and found real drift: PAD-Team (106) missing Evan Jenkins vs. main Team, and PAD-Purchasing (107) had a genuinely corrupted entry (`<rsm_email>mgoldyn@allsurfaces.com` jammed into one field, not two rows) — fixed via a plain `UPDATE` back to `mgoldyn@allsurfaces.com` per user's explicit choice (match 105 exactly, no RSM there). User then clarified the real ask was **Prod**, not Play — re-checked there: PAD-Purchasing (107) was missing 5 of 9 recipients vs. main (105) (Jerome Butler, Justine Daugherty, Alex Sivongsay, Sales Rep token, Order Taker token), PAD-Team (106) missing Evan Jenkins vs. main (104). Recommended the user add these via the client rather than raw SQL INSERT (this exact table, `alert_recipient`, has a documented counter-drift history from raw inserts, plus the user's own stated preference from 8/3: alerts built via SQL have caused real problems before) — **user added them; re-verified: all 4 alerts' recipient sets now match exactly** (Team: 6/6, Purchasing: 9/9). All four (104/105/106/107) remain `705` inactive — no live-fire risk from any of this. **Evan replied: go-live confirmed for 9/4/2026** (updated from the 9/2 he'd originally floated). Reminder scheduled for 9/3 (day before) to do final prep and flip Prod `row_status_flag` 705→704 on all four alerts. Next: on 9/3, confirm recipient set still holds, decide on BCC-to-self (raised as a want, never actually built — quick add-or-defer call), then flip live | `project_2026_07_13_low_margin_alert.md` |
| 48715 | 2026-07-27 | Five Way Params (SSRS) | 🟡 Awaiting Jim's validation | Null defaults on the `-SA-48715-Fix` report **verified correct** (user checked the Parameters page 2026-07-27 — all lookup params default Null, dates expression-based, bools False, `columnA` intentionally no default). Fix is complete; only outstanding item to close is validation by the requester. **Jim Geerling has not run the `-SA-48715-Fix` report in the retained 60-day window** (confirmed via `ExecutionLog3`). Reminder email drafted to `jgeerling@allsurfaces.com` (confirmed P21 contact 1079) — saved in **Outlook Drafts, not sent**. Next: send it; on his reply, close SA-48715 | `project_2026_07_13_sa48715_five_way_params.md` |
| 49504 | 2026-07-13 | EDI 855 view fix | 🟡 Awaiting feedback | View + v4 .srd deployed to Prod, both cols verified. Awaiting user feedback | `project_2026_07_13_edi_855_view_fix.md` |
| 48124 | 2026-07-28 | Vendor Invoice EDI Charges | 🟡 Pending A/P validation | **Built and Play-tested.** `asi_view_vendor_invoice_edi_charges` (header-grain SAC charges + `invoice_amount`) deployed to **Play AND Prod** with grants (`p21_application_role`/`PxxiUser`). Crystal subreport `sub_AdditionalCharges` added to `P21VendorInvoiceReport.rpt` (**Play only**): itemizes charges in the invoice footer + "Invoice Total (incl. charges)" driven off `invoice_amount` (baseline report view omits it), suppresses cleanly when no charges. Verified Play: 26172056 → Drop Charge 41.56 / total 597.16 (was 555.60); freight-only 26171989 → no box. Traps solved: new view had no grants → ODBC-RDO 500'd as "Failed to open the connection" (misleading); subreport must share the main report's `P21.Allsurfaces.com` connection (P21 injects crystal creds only there). Emailed Chad w/ 5 sample PDFs. Next: A/P sign-off → deploy `.rpt` to Prod → final check invoice **26190433** → Drop Charge 1.05 / total 76.53 | `project_sa48124_vendor_invoice_edi_charges.md` |
| | 2026-06-26 | jsTextValidator / B2B popup | ⏸ Parked (open Q) | Traced popup to jsTextValidator→js_proc_string_fix err 997-5. OPEN: is B2B 80-char/no-symbol limit still valid? (gates asi_po_entry_text_validator rewrite) | `project_2026_06_26_js_text_validator_b2b.md` |
| 47981 | 2026-08-10 | WWMS Pick Item Desc | ⏸ Parked — lost in refresh, not re-registered | **DISPLAY WORKS — earlier "unreachable/needs Epicor" verdict retracted.** Converter `_populate_t2` renders `ufc_p21soc_wwmsitemdesc` on screen (verified: CRA197 → "Crain 197 Comfort Knees Knee Pads", screenshot). Only blocker was **timing**: the field paints when the Window-Opening fire lands **with the line's data bound**, which happens on the bin-selection re-fire of `w_ship_sheet_rf`. **2026-08-10:** the P21BusinessRules refresh-from-Prod deleted both registrations (`_populate_t2` uid 168, `_t3` uid 165) — `business_rule` was never captured/restored (see the BRR refresh recovery entry below). User chose **not** to re-register for now; WWMS work is on hold indefinitely. Source unaffected in `CSharp\`. If resumed: re-register fresh in the P21 client (nothing to restore from backup), then run the original line-advance test — does the desc follow every line, or only after the bin action? | `project_2026_06_25_sa47981_wwms_pick_item_desc.md` |
| | 2026-06-23 | kb_Order_Validator_v2 | 🟡 Follow-up test | P21 ACCEPTED deploy 2026-06-24. Built OLD-vs-NEW harness. Resume at A1 finder → run case A1 in Play | `project_2026_06_23_kb_order_validator_v2.md` |
| | 2026-06-16 | QTO Taker BR | 🟡 Partial | Wizard path ready for Prod. RMB path on hold (P21 message-box event has no order context); emailed Chad & Tyler 2026-06-23 | `project_2026_06_16_qto_taker_br.md` |
| | 2026-06-17 | Portal cleanup (Goal 1) | 🔵 In progress | 156 unused .srd + 32 assets staged; to-dos #2–#5 open (unassign users/roles). Read `C:\_P25\Portal-Cleanup-Progress.md` on resume | `project_2026_06_17_portal_cleanup.md` |
| | 2026-07-31 | fcB2B Quarterly 810/850 Metrics — FYQ4 2026 (Jul-Sep 2026) to WFCA | ⏸ Parked | Not started — quarter doesn't close until 2026-09-30. **Due ~2026-10-12** (also tracked by Outlook Task "Send f2cB2B report Q4 on 10.12.26" — its due date was found unset and fixed to 2026-10-12 during 2026-07-31 wrap-up). Run `Create-B2BMetricsReport.ps1 -FiscalYear 2026 -Quarter 4`, verify against process guide, email Lewis Davis (`ldavis@wfca.org`) via Outlook | `Deploy-Guides\fcb2b-quarterly-810-850-report.md` |

## Completed

| Ticket # | Date | Task | Detail |
|---|---|---|---|
| 52768 | 2026-08-28 | Problem order Centaur — large-order $15K sign-off bypassed on order 5977563 (Pamela Dundas) | Traced order 5977563's full audit trail: a Centaur line was mistakenly bumped 200 → 20,000 units (~$68,800) on 7/14, uncaught for a month, corrected 8/13. Root cause: `kb_Order_Validator_v2` → `dbo.kb_fnt_br_order_validator_v2` ("PREVENT SAVE SCENARIO 7") tests a note's free-text **Topic** for the literal string "Signature Required" instead of the actual notepad class (`SIG-15K` Large Order vs `SIG-NS` Non-Stock vs `SIG-TAR` Tariff) — an unrelated Non-Stock signature note already on the order satisfied the check and the save-block never fired. Confirmed to Matt. Full trigger inventory sent 8/25: exactly 3 note classes exist but only Scenario 7 ($15K) is system-enforced; Non-Stock/Tariff are policy-only. Account-number question ruled out (screenshot didn't match the order's real data; no `customer_id` change in the audit trail). Both objects are kb_-authored and still active — `asi_` migration already exists but is blocked on the Atlas surcharge DLL CAS error (`project_2026_06_23_kb_order_validator_v2`). **Management decided 8/28 not to fix this as a standalone item** — it folds into the business rule cleanup project backlog. Scoped fix (class-specific note check + Centaur-item-triggered check regardless of $ total) documented in the project memo, **not built**. Plain-language email to Pamela Dundas sent (user sent manually after an Outlook COM `Send()` "inline response" bug — see `feedback_outlook_send_inline_response_error.md`); **Pamela replied OK to close. Ticket closed, no code produced.** | `project_2026_08_22_sa52768_centaur_order.md` |
| 53445 | 2026-08-28 | SSRS "Sales by Division Report & Financial Overview Dashboard" 8/24 5:00 AM email reported not received (Jere Butler) | The "two reports" are **one** SSRS subscription — a single daily `TimedSubscription` on `/Automated Reports/Financial Overview/Financial Overview Dashboard` (PDF email, 10 recipients + BCC mmunson); the subject `"Sales by Division Report & Financial Overview Dashboard for @ExecutionTime"` is just a label, no separate report/subscription exists anywhere on the server. **The 8/24 run succeeded** — `ExecutionLog3` shows start 5:00:03 / end 5:04:26, `rsSuccess`, 515,337 bytes / 19 rows, identical to every day 8/20–8/28. SSRS logs the render independent of the SMTP hand-off (~5:04). **SSRS mail relays through `relay.allsurfaces.com` — the same relay P21 uses** (P21 prod DB Mail "P21 Alerts" → `relay.allsurfaces.com:25`), so a shared-relay outage behind a "P21 mail issue" delays SSRS emails too while SSRS still logs success. `dbo.Subscriptions.LastStatus` is overwritten each run (8/25–8/28 wiped 8/24's send line); definitive per-send record is `ReportServerService_<date>.log` on ASDWDB01 (no file access). Report has no parameters / `ExecutionFlag=1` / no `dbo.History` snapshot — a past date can't be reproduced, only re-fired with current data. **Resolution: the 8/24 email arrived shortly after the ticket opened — delayed, not lost, consistent with the shared-relay theory. Ticket closed.** Outlook draft reply to Jere + clipboard ticket-note prepared mid-session, made moot by the resolution. | `feedback_ssrs_p21_shared_mail_relay.md` |
| | 2026-08-27 | P21 Package Type "Tare Weight" field — how it works (no ticket, Purchasing via Mike Learned) | Answered from a live P21Play investigation. Field is `item_package_type.item_pkg_tare_weight` (`decimal(19,4)` NOT NULL, defaults `0.0000` — never NULL; the "decimal(4)" dictionary blurb is P21 shorthand for scale=4). It's the **tare / packaging-material weight**, despite the stock dictionary text. `item_package_type` stores only tare weight + volume as editable per-row values; GROSS WEIGHT / WEIGHT UOM / VOLUME UOM in the Item Maintenance grid are **derived, display-only** in `w_inventory_sheet` (gross ≈ `inv_mast.net_weight` × qty + tare). Two-level: `package_type.package_tare_weight` is the global default per type (Play: Pallet 45.0, Carton/Each 0), `item_pkg_tare_weight` is the per-item override, resolved `COALESCE(item_pkg_tare_weight, package_tare_weight)`. **Downstream (full dependency scan of every view/proc/function/trigger/alert in Play): only 4 objects reference it, all the Tag / WWMS labeling subsystem** (`p21_view_item_package_type`, `p21_tag_weight_volume`, `p21_rebuild_tag_from_bin`, `p21_rebuild_tag_from_lbxref` → `tag_hdr.tag_weight` rollups). No business rules, no alerts, no FK/trigger locks. Populating it affects **printed tag / license-plate / WWMS pallet & carton label weights only** — not pricing, costing, commissions, order entry, packing lists, or freight rating. **Grayed-out field root cause — user found it mid-session:** the tab is a child grid binding to package types defined in **Package Type Maintenance** (`package_type` table); the package type (`PK`) had no row / no values set up there, so the override was read-only. `kb_`/`js_` scan of the chain: none, clean. No repo artifact, no deployment. Closed | `reference_p21_tare_weight_package_type.md` |
| 53270 | 2026-08-27 | New portal "BR Will Call Orders" (John / Jere — Will Call order cleanup) | Mirrors `BR SAL WILL ADVISE` minus Cost/Extended Cost, plus Promise Date and a **Disp** column (`oe_line.disposition`, after Qty Ordered). Carrier = Will Call (`carrier_id = 10002`, verified identical Play/Prod), branch-restricted via `asi_fnt_get_user_loc('<user_id>')` (swapped from `kb_fnt_get_user_loc` — already a proven 2026-04-14 replacement in both envs, byte-identical via `EXCEPT` both ways; **zero kb_ deps**), Order # drillable via `dc_nav_drill` (uid 152 Play), `ORDER BY order_date DESC`. First hand-spliced `.srd` passed every static check but crashed P21 on open (PowerBuilder couldn't *build* the DataWindow — `SelectElement`/`SelectPage`, no SQL error); root-caused by isolating variables (ruled out SQL complexity, then UI-server caching via a fresh-name registration) and **rebuilt correctly in InfoMaker**. 8/25: fixed Promise Date column (full timestamp → `mm/dd/yyyy`, width trimmed) via a pure attribute edit. **8/27: Matt approved; user deployed to P21 Prod for roles All, Branch Manager, Management** (Management added specifically for Prod, per Matt) and centered Unit Price to match Extended Price. Ticket-update note → clipboard; Outlook draft reply to Matt ("closing SA 53270 as completed", left unsent in Drafts). **Not independently verified in Prod:** portal_element_uid, that the Prod `.srd` carries the Disp column, and deletion of the Play TEST2 diagnostic (portal_element_uid 326 + `br_will_call_orders_test2.srd`). **Repo lag:** `Portals\SA-53270\br_will_call_orders.retrieve.sql` + `br_will_call_orders.srd` were not re-synced with the Disp column — deployed file is ahead; follow-up tracked in the deploy guide. Closed | `Portals\SA-53270\`, `project_2026_08_22_sa53270_br_will_call_orders.md` |
| | 2026-08-27 | Delete Derek's stray "Test" DynaChange `screen_orders_hdr_date_popup_all` in Prod (no ticket) | A web/Extensibility "Screen" DynaChange (the `design`/`assignment` framework, not the desktop `custom_objects`/`fc_dataobject*` one) created by Derek Sievertsen (departed) — description was only "Test", it was assigned to role **All**, and it **hid the option to update the required date** on the Order Header date popup. Surfaced during a DynaChange review; emailed Derek 2026-08-26 ("Request to delete Dynachange") asking if it was still needed — he replied it was not. **User deleted it from Prod 2026-08-27.** `Docs\P21-Env-Status-Customizations.txt` Prod section updated. No kb_/js_ objects involved. Closed | `Docs\P21-Env-Status-Customizations.txt` |
| | 2026-08-27 | Claude Code launch setup for VS Code — `cc` alias + folderOpen task (no ticket) | `cc` (a `Start-Claude` function: Push-Location `C:\Claude` → `& claude @args` → Pop-Location) worked in Windows Terminal but not the VS Code terminal because PS7 loads a different **host profile** per host (`Microsoft.PowerShell_profile.ps1` vs `Microsoft.VSCode_profile.ps1`) and the block lived only in the former. Fix: moved the block into `profile.ps1` (CurrentUserAllHosts, was empty), which loads for every host; removed the duplicate from both host files, left a breadcrumb comment in each. Those 3 files are OneDrive-redirected `Documents\PowerShell\` — outside this repo, not in the commit. Also added `C:\Claude\.vscode\tasks.json` (`runOn: folderOpen` → dedicated `claude` terminal on folder open; needs a one-time "Allow Automatic Tasks"). Screenshot's "Language model unavailable" panel was GitHub Copilot Chat, not Claude — unrelated. 🟢 Done — `cc` works in both hosts after a profile reload | `.vscode/tasks.json`; `feedback_powershell_host_specific_profiles.md` |
| 53301 | 2026-08-27 | RMA tab column rename on Transaction Master Inquiry — "Lost Sales Desc" → "Reason" (follow-up to SA 51376) | Renamed via direct SQL rather than the client GUI — the header isn't a simple override row, it's embedded inside a serialized PowerBuilder `create text(...)` blob (`custom_objects_detail`, object `ufc_lost_sales_lost_sales_desc_t`); fixed via a scoped `REPLACE` on that string, plus `fc_dataobject_column.description`, `version_desc` changelog, and audit columns synced. Built and verified in **P21Play** first (2 roles), then dry-ran the generalized Prod script against **P21BusinessRules** (refreshed from Prod 8/25) before touching real Prod — caught a real bug in the process: `version_desc` (`varchar(255)`) overflowed on role `uid 2990` (Vendor Maintenance, already 208 chars), rolling back the entire set-based update with zero rows changed. Confirmed the identical 208-char value existed in live Prod too, so this would have failed there unmodified. Fixed with a shortened note + `LEFT(...,255)` guard, verified end-to-end in Business Rules (DB + P21 client, after clearing its cached DynaChange layout). Jessica & Chad signed off on the Play change 8/26; **user ran the fixed `Update-SA53301-RMA-ColumnHeader-Prod.sql` against real Prod 2026-08-27, confirmed complete, no errors.** Closed | `Sql-Scripts\Update-SA53301-RMA-ColumnHeader-Play.sql`, `Sql-Scripts\Update-SA53301-RMA-ColumnHeader-Prod.sql`, `Sql-Scripts\Update-SA53301-VersionDesc-Fix-BusinessRules.sql` |
| | 2026-08-25 | Default label printer — set `default_label_printer='print.allsurfaces.com'` where `delete_flag='N'` and null, P21BusinessRules `users` table (no ticket) | Checked prior art first: `Sql-Scripts\Labels\Update-P21-Users-LabelPrinter.sql` sets the same value but for 2 named users in Prod (different scope/env); `Update-Deleted-Users-Flag.sql` gave the established preview→update→verify pattern for `P21BusinessRules.dbo.users` incl. audit-column convention. Built `Sql-Scripts\Update-P21BusinessRules-DefaultLabelPrinter.sql` on that pattern. No kb_/js_ objects involved; no perf concern (small table, single-table update). User ran it, reported complete, no errors | `Sql-Scripts\Update-P21BusinessRules-DefaultLabelPrinter.sql` |
| | 2026-08-25 | Deleted-user cleanup — flag `**`-marked departed-employee users `delete_flag='Y'` in P21 `users` table (no ticket) | Checked schema first (`delete_flag`, not `deleted_flag`) and found `**` is a real, established naming convention (`** LEFT <date> ** <person>`), not a placeholder — 479 rows in `dbo.users` match, 463 already flagged, scoped the script to just the real 16-row gap (`active='N'` but `delete_flag` still `'N'`). Idempotent preview→update→verify script: `Sql-Scripts\Update-Deleted-Users-Flag.sql`. User ran it in P21BusinessRules, reported no issues; verified independently via direct SQL rather than trusting that at face value — re-queried and confirmed 479/479 now `delete_flag='Y'`, 0 remaining. One open watch item, not a defect: `EPICORP21USER` (`** Req for IIS ** P21 Web App`) matched the same pattern and got flagged deleted too, but reads like a service account rather than a departed employee — worth confirming the P21 Web App/IIS identity doesn't break if anything checks `delete_flag`. Scope was BusinessRules only; not run in Play/Dev/Training/Prod | `Sql-Scripts\Update-Deleted-Users-Flag.sql` |
| | 2026-08-25 | SSRS mail-relay incident — "Inventory Usage 90 Days" ~90MB email hung on relay (no ticket) | Traced ownership via SSRS REST API v2.0: 5 subscriptions on the live report, 4 are legit recurring business sends, the 5th — a stray daily-1AM test subscription owned by `mgoldyn` himself since 2025-07-14 ("Test of missing Paramater") — matched the incident. Confirmed via `ExecutionLog3` on `ASDWDB01.ahi.local`\`ReportServer` it rendered 72.8MB/49,236 rows, 40x larger than anything else sent that night. User disabled it; confirmed via REST API no other stray subscriptions exist under his name anywhere on the server. Built `Get-SSRSSubscriptionSummary` (new function in both PowerShell profiles, queries `ExecutionLog3` by size, filters dataset noise, warns ≥20MB) so this is easy to catch proactively — tested live, not yet deployed to the live `$PROFILE`. Resolved | `reference_ssrs_report_server.md`, `feedback_ssrs_stray_test_subscription.md` |
| | 2026-08-14 | WWMS/web DynaChange schema mapping — where `WWMS_screen_tab_oder` and Version Manager customizations actually live in SQL (no ticket) | Mapped a second, separate DynaChange mechanism (`design`/`assignment`/`roles`/`modification`, the web/Extensibility framework used by WWMS/RF screens and the Version Manager UI) distinct from the desktop PowerBuilder one (`custom_objects`/`fc_dataobject*`). Proved it two ways: traced `p21_dynachange_info_web`'s proc body for the table names, then confirmed live via a one-off Extended Events trace of Version Manager loading a real design plus a byte-for-byte match against UI screenshots. Found the two mechanisms are actively cross-checked at runtime (`custom_objects.design_uid`, previously assumed dead, gets queried live by Version Manager) and that Version Manager's field-picker reuses the desktop Field Chooser catalog. Delivered a reusable query script and a plain-English Word summary of the four live "Screen" DynaChange versions (all RF/WWMS handheld-scanner tab-order/field-lockdown changes, SA-38188 family). Trace session torn down and confirmed gone at close | `reference_p21_wwms_web_dynachange_schema.md` |
| 52221 | 2026-08-13 | Branch 261 cycle counts return "nothing to retrieve" | Two stacked root causes found via live SQL Extended Events trace (not guessed from schema): (1) location 261's `cycle_count_loc_criteria` was soft-deleted (`row_status_flag=700`) since 8/3 by acampbell — the only one of 47 Prod locations in that state, blocking all cycle counting for the branch regardless of mode; (2) undeleting it via the P21 client resets Count By to "Bin" rather than restoring the original "Item By ABC Class," which then fails separately since 261's items aren't bin-tracked. Fully proven end-to-end in Business Rules (test item `GRO58080`); fix (restore Count By) given for Prod but not personally applied/verified there. Per user, ticket closed | `project_2026_08_13_sa52221_cycle_count_261.md` |
| 51844 | 2026-08-12 | Employee Change: Edilberto Rovello promoted to 1st Shift Warehouse Manager, Wood Dale (Randy Wayman, requested 8/4) | Access change applied to EROVELLO in Prod (P21 client, outside a Claude session). Re-verified with `asi_proc_compare_p21_users` against Prod: every access field from the original diff (`role_uid`, `po_approval_threshold`, 12 OE flags, 4 app-security rows, `power_bi_user`, `ops_region`) now matches RWAYMAN; only identity fields remain different. User confirmed access is now the same. File-share access (Ops Warehouse/Branch Manager files) and title update remain unconfirmed, outside this closure | `project_2026_08_06_sa51844_user_compare.md` |
| | 2026-08-12 | Find WIRELESS_SHIPPING_LABEL .rpt files across P21 CrystalReports shares (no ticket) | Re-ran `Find-WirelessShippingLabel-RptFiles.ps1` directly and confirmed it works end to end: 10 shares via `net view`, 7 `CrystalReports` folders found, `WIRELESS_SHIPPING_LABEL_user.rpt` located in Prod, 12 total matches incl. backup/legacy copies across other envs | `PowerShell\Find-WirelessShippingLabel-RptFiles.ps1` |
| | 2026-08-12 | WWMS Alert "Open" throws `Window <<m_binreplenishment>>` error (BRR, post-refresh) | Root cause was AHI-API1's P21 UI Server caching menu/security in-process from pre-refresh DB state. User confirmed the IIS app-pool recycle (API-P21BusinessRules - P21 SOA site) fixed the Bin Replenishment "Open" button. Standing follow-up not yet acted on: add the app-pool recycle as a step in the After-refresh checklist | `project_2026_07_20_wwms_alert_binreplenishment_error.md` |
| | 2026-08-12 | 90 Day Usage Report — blank reports on several branches (no ticket, Jami Klitzman) | Durable `mfg` "Use Default Value" fix (7/27) validated: queried `ReportServer.dbo.Subscriptions` on `ASDWDB01` directly, all 44 per-location subscriptions show the normal 8/9 Sunday run with `LastStatus = "Mail sent..."`, no errors. Closes the reported blank-subscription symptom. Two side-threads remain genuinely open but deprioritized: the intermittent zero-row `ExecutionLog3` bug (mechanism never found) and the recommended `product_group`/`mfg` NULL-guard query fix | `project_2026_07_23_ssrs_90day_usage_blank.md` |
| | 2026-08-11 | Postman bearer-token setup for ZillantSvc-Dev service account (no ticket). Root cause of the original "user/password incorrect" was environment confusion, not bad creds: `ZILLIANTSVC-DEV` exists only in BRR's `users` table, but the "AS BRR" Postman environment's `base_url` was actually pointed at Prod. Built a separate `AS BRR - Zilliant Dev` Postman environment (`Postman\AS-BRR-Zilliant-Dev.postman_environment.json`, gitignored) with the correct `p21businessrules.allsurfaces.com:3444` endpoint. A second, unrelated bug (base_url edited to right port/wrong hostname) caused a TLS-handshake-level failure — root-caused via Postman Console after ruling out DNS/TLS/proxy/HTTP2. **User confirmed the token request now works** | `project_2026_08_11_zilliant_svc_dev_postman.md` |
| | 2026-08-11 | `pc_user_def`/`USER` counter drift in P21BusinessRules — Add User failed with `pk_pc_user_def` PK violation. Traced the real environment via `message_log_uid` (not Prod, despite ambiguous phrasing). Confirmed `seq_USER` (drives `pc_user_def.user_skey`) was 2 behind the table's real max, likely from the 8/10 refresh. Standard `p21_set_counter` fix failed (`counter` row for `id='USER'` has no mapped table); resolved with `@expert_mode=1` + explicit `@counter_num`. User confirmed fixed | `feedback_p21_counter_drift.md` |
| | 2026-08-24 | Alert counter drift in **P21 Prod** — Add Recipient failed with `pk_alert_recipient` PK violation (SQLDBCode 3621, duplicate key 197). Built a read-only check (`Check-Alert-Table-Counters-Prod-ReadOnly.sql`) across all 8 alert-related tables, confirmed `alert_recipient` counter (198) behind real max (205), and caught 3 more drifted tables in the same pass that hadn't errored yet: `alert_implementation` (103→107), `Alert_implementation_query` (240→272), `alert_message` (106→110). Fixed all 4 via `EXEC p21_set_counter @set_to_table_value=1` (`Fix-Alert-Table-Counters-Prod.sql`), re-ran the check, all 8 tables confirmed OK. 4th confirmed instance of this pattern | `feedback_p21_counter_drift.md` |
| | 2026-08-11 | Item Maintenance "Modify statement failed" — item description quote trap (no ticket). Root cause confirmed: unescaped `'`/`"` in `item_desc` breaks the base window's `info_t.Text` Modify() expression on the SQL Information dialog (General **and** Replenishment tabs, both use `d_dw_sql_information_report`) — not a DynaChange customization (verified empty across `custom_objects`/`fc_dataobject*`; Screen Designer opens as "NEW"). Matches known Epicor defect **KB0125402**, corrected in version **2022.2.4773**. **Confirmed our Prod version is 21.1.4559 — below the fix**, so no case to open (Epicor's case-opening guidance only applies at/above the fix version); the only remedy is a future P21 version upgrade. Affects ~64,276/138,120 active items (~47%). Emailed Efrain Reyes (heads-up + KB0125402 attached) — **sent 2026-08-11**. **Closed** — nothing further actionable until a P21 upgrade is scheduled | `project_2026_08_11_item_maintenance_modify_statement_failed.md` |
| 52357 | 2026-08-11 | Add Replenishment Location field (Jerome Butler) — both green tabs (`By Supplier ID`/`By Location`) in the Location-Supplier Item Order Cycle Current Forecast workbook are live Power Query tables against `p21_view_inv_loc`; added `l.replenishment_location` to both M formulas via COM (no new join needed, table already in FROM clause), saved without a headless `RefreshAll()` (parameterized-query hang risk), had user refresh manually and confirmed the column populated. SysAid note + Outlook reply (threaded, refreshed file attached) sent to Jerome, cc Mike Learned/Jossy Vadakkel/Jami Klitzman. Jerome confirmed and closed the ticket | `project_2026_08_11_sa52357_replenishment_location.md` |
| | 2026-08-10 | P21BusinessRules refresh — recovered the `business_rule` protection gap flagged 8/7. Refresh ran before the planned Phase 7 automated capture was built, deleting all 4 BRR-only rule registrations as predicted. Manually restored the Order Ack pair: recreated `asi_email_context_flag` table + grants (`Create-asi-email-context-flag.sql`), re-registered both `asi_email_context_flag` (FormPreEmail, Synchronous, verified single clean registration) and `asi_oe_email_close_diag` (cb_ok/w_email_response, verified single clean registration though it came back Synchronous instead of the previously-proven Asynchronous config — flagged, not assumed safe), then proved it end-to-end with a real controlled test send (marker received). SA-47981 WWMS pair deliberately **not** re-registered — user's call, work is on hold (see that row above). Also found (not fixed) an unrelated bug in the After-script's new location-name verification step: `Invoke-Sqlcmd` returns a bare (non-array) object on a 1-row match, so `$rows.Count` throws instead of reporting which location was missing its suffix — data itself was confirmed clean via direct query. Phase 7 (automated `business_rule` capture/restore) itself is still not built — left in Backlog | `project_p21businessrules_refresh.md` |
| | 2026-08-07 | Sync P21 Prod-deleted users to all 4 lower environments — 37 users (Play 13, BusinessRules 18, Dev 3, Training 3) deleted via `asi_proc_delete_p21_user`, verified 0 remaining across all 4. Also fixed `Delete-P21-User.ps1` to call `asi_proc_delete_p21_user` instead of the retired `kb_proc_delete_user` | `Sync-DeletedUsers-LowerEnvs.ps1` |
| | 2026-08-07 | P21Training alert testing unblocked — 5 drifted alert-table counters (`alert_recipient` was the one causing `SQLDBCode 2627`) fixed via `p21_set_counter`; P21Training alert_message env-tagging added (44/44 rows), matching the protection Play already had | `Sql-Scripts\Check-Fix-Alert-Table-Counters-Training.sql`, `Sql-Scripts\Update-P21Training-Alert-Message-Env-Tag.sql` |
| | 2026-08-06 | Remove `ds_view_open_rma_value` (legacy/non-P21 view cleanup, spun off from SA-51376) — **Prod complete**, prioritized ahead of finishing the remaining 4 Play roles since Play/Training pick up the fix automatically on their next refresh-from-Prod. All 6 roles (158/922/923/929/930/2990): old field removed, shared definition retargeted to native `p21_view_open_rma_report`, field rebuilt via Field Chooser, 9→2 decimal format fixed, changelog logged — each step verified via SQL, not assumed | `Deploy-Guides\ds_view_open_rma_value-retirement.md` |
| 51376 | 2026-08-06 | RMA Transaction History — Reason Code column shipped to Prod on all 21 role versions of Transaction Master Inquiry Orders (RMA tab). Data source: `lost_sales_transaction` (code 2145) LEFT JOIN `lost_sales`, header-grain, populates even for Open/unreceived RMAs. Requestor sign-off received 2026-08-06. Supplemental index (`ix_lost_sales_transaction_transaction_no`) built and measured in both Play and Prod: 2,905→3 and 2,951→3 logical reads (scan→seek). Field built via Field Chooser New Field on `_all`, then placed via existing definition on the other 20 roles — verified via SQL across all 21 (field present + join-key column hidden). Found and fixed 2 roles (`system admin`, `product manager`) where the hidden join-key column was left visible (`width=500` instead of `-1`) — corrected, re-verified clean | `Deploy-Guides\SA-51376-rma-transaction-history-reason-code.md` |
| | 2026-08-04 | Fix + deploy `asi_proc_copy_p21_user` to all 6 P21 envs (retiring `kb_proc_copy_user`), fix PowerShell wrapper `Add-P21User-v1.ps1`. Fixed 3 bugs (description/job_title not inheriting from source, "Role" mislabeled as job_title instead of the real security role, blank-vs-NULL handling on Salesrep/Buyer ID) + added Buyer ID to the summary output; deployed under one consistent name to Prod/Play/Dev/BusinessRules/Training/Upgrade with grants matching the Training copy; renamed Upgrade's duplicate `asi_copy_p21_user`; fixed and verified the PowerShell wrapper that was calling a nonexistent proc name and expecting output the proc never produced. User is adopting as the standard tool going forward | `SQL-Schema\CREATE-OR-ALTER-PROC-asi_proc_copy_p21_user.sql` |
| | 2026-08-04 | Revoke departed employee (kbenish) access — found live `kb_proc_copy_user` EXECUTE grants for `kbenish`/`AHI\kbenish` in all 6 P21 environments including Prod while investigating the item above; user confirmed she no longer works here. Revoked in all 6, verified clean. Corrected a false lead (stale archived log wrongly suggested she owned SQL Agent jobs — live `msdb.dbo.sysjobs` on both instances showed she owns none) | `feedback_kbenish_departed.md` |
| 51571 | 2026-07-31 | Contractor P21 Play account — Neil Van Walbeck. Created manually as `NVANWALBECK`, Role `All`, since `asi_proc_copy_p21_user` (the `kb_proc_copy_user` replacement) has an open bug. **Closed 2026-07-31** | `project_2026_07_31_sa51571_neil_vanwalbeck_contractor.md` |
| | 2026-07-31 | fcB2B Quarterly 810/850 Metrics — FYQ3 2026 (Apr-Jun 2026) sent to WFCA (Lewis Davis, `ldavis@wfca.org`). Also rebuilt the whole pipeline: added `-FiscalYear`/`-Quarter` params to `Create-B2BMetricsReport.ps1`, merged the two 810-side queries into one (62,669 → 1,454 logical reads, measured), and found/documented two historical data-quality issues (FYQ4 2025 mislabeled year, FYQ3 2025 cumulative-looking numbers) | `Deploy-Guides\fcb2b-quarterly-810-850-report.md` |
| | 2026-07-31 | KB Usage Report false-negative fix — `Export-KBUsageReport.ps1`'s Objects tab was flagging live, nightly-scheduled procs (`kb_proc_apply_future_costs`/`kb_proc_apply_future_prices`) as unused because it relies on plan-cache DMVs that reset on recompile/eviction, unrelated to actual usage. Added an `msdb.dbo.sysjobsteps` cross-reference so a scheduled object now shows amber ("blind spot, check the job") instead of red ("looks safe to retire"). Verified against live Prod (341 objects) and re-ran the full report before a planned server reboot | `PowerShell-Scripts\Export-KBUsageReport.ps1` (commit `c391d60`) |
| 48732 | 2026-07-29 | CSR Open Order Team — deployed to Prod 2026-07-27 (view, `.srd`, portal container, assigned to role `ALL`), verified populating. **Closed 2026-07-29 — no issues reported.** Taker-column confirmation & kb-tracker logging left as non-blocking follow-ups | `project_2026_07_13_sa48732_csr_open_order_team.md` |
| | 2026-07-15 | Efrain — Discontinued items + on-hand qty query (fixed C2 alias bug; delivered inline) | `Sql-Scripts\Discontinued-Items-With-QOH.sql` |
| | 2026-06-22 | Portal Tab Tracking XE deployed (Play + Prod) | `project_2026_06_22_portal_tab_tracking.md` |
| 48237 | 2026-06-18 | asi_view_inv_mast built (P21Dev) | `project_2026_06_18_asi_view_inv_mast.md` |
| | 2026-06-15 | Compare-P21-Users.sql built | `project_2026_06_15_compare_p21_users.md` |
| | 2026-06-15 | P21 Dev refresh | `project_p21dev_refresh.md` |
| | 2026-06-11 | asi_ribbon_rm_default_products committed | `project_2026_06_11_ribbon_default_products.md` |
| | 2026-06-02 | BRR refresh fixes (Phase 0D crash, grants, Training proc deploy) | `project_2026_06_02_brr_refresh_fixes.md` |
| | 2026-06-02 | Suppress Messages business rule → Prod | `project_suppress_messages_business_rule.md` |
| 45138 | 2026-05-28 | BOP portals V2 + drill-through → Prod (approved by Pamela) | `project_sa45138_bop_portals.md` |
| | 2026-05-27 | P21 Play refresh (EDI-aware settings v2026-05-27) | `project_p21play_refresh.md` |
| 45865 | 2026-05-19 | apc_business_rule_extensions_xml varchar fix | `project_apc_permissions.md` |
| | 2026-05-14 | Check-Job-History daily failed-job checker | `project_check_job_history.md` |
| | 2026-05-01 | P21Training DB refresh scripts | `project_p21training_refresh.md` |

<!-- Older sessions (pre-May 2026) are captured in memory; add rows here only for notable shipped work. -->

## Backlog — requests, side projects, nice-to-haves (not yet started)

**No status here — these haven't started.** When you pick one up, cut the row and add it to **Active** with a status.

| Added | Item | Type | Priority | Who / why | Notes |
|---|---|---|---|---|---|
| 2026-07-15 | Raise `cleanupPeriodDays` so `/resume` reaches older sessions | Nice-to-have | Med | Me — transcripts prune at ~30d, killing older resume points | Config change in settings.json; notes remain the durable layer regardless |
| 2026-07-15 | Cleanup deleted salesreps (ribbon default products) | Side project | Low | Follow-up from `asi_ribbon_rm_default_products` (3c76bac) | See `project_2026_06_11_ribbon_default_products.md` |
| 2026-07-15 | Confirm B2B 80-char / no-symbol limit still valid | Request (open Q) | Med | Gates the `asi_po_entry_text_validator` rewrite | Blocks the parked jsTextValidator task; see `project_2026_06_26_js_text_validator_b2b.md` |
| 2026-07-15 | Ticket 47715 — waiting on user | Request | Med | External ticket; waiting on user reply | Keep: 3 Goals |
| 2026-07-15 | Purch open-PO EDI portal — update `kb_ref` | Request | Med | **kb_ flag** — retire the kb_ dependency | Keep: Project Focus |
| 2026-07-15 | Fix CMI Open Orders & RMA tab — more SQL fix | Side project | Med | Follow-on SQL fix | Keep: BR list |
| 2026-07-15 | Combine all ribbon BRs into one rule (start with Order-Entry ribbon) | Side project | Med | **Check kb_/js_ in the ribbon rules**; overlaps the `asi_ribbon_cm_SYTD` YTD work | Keep: BR list |
| 2026-07-15 | Update refresh scripts to include alerts/emails | Side project | Med | Refresh-script enhancement | Keep: To do |
| 2026-07-15 | P21 Upgrade — check PO fix / latest UnApproved-PO portal; how to update POs (for Jere) | Side project | Med | Q2 upgrade focus; Jere | Keep: P21 Upgrade Q2 |
| 2026-07-15 | Check with Tony about P21 emails | Request | Med | Waiting on Tony | Keep: To do |
| 2026-07-15 | Set up weekly Compare-folders job (Fridays) | Side project | Low | Automate the folder compare | Keep: To do |
| 2026-07-15 | sp conversion for Jossy — add examples | Request | Low | Jossy | Keep: To do |
| 2026-07-15 | Rename wireless portal; add to module & limit to two weeks | Side project | Low | Portal task | Keep: To do |
| 2026-07-15 | "scan * pack" in audit trail — look deeper | Side project | Low | Investigation | Keep: To do |
| 2026-07-15 | Price-change tracking — any source other than audit trail? | Request (open Q) | Low | Investigation | Keep: To do |
| 2026-07-15 | P21 user-def tables — is it possible to check? (ask Claude) | Request (open Q) | Low | Investigation | Keep: To do |
| 2026-07-15 | Suspended Alerts status (ref 1/5/26) | Request | Low | Alerts review | Keep: Project Focus |
| 2026-07-15 | Create Confluence doc for non-regular tasks (Rewards alerts, holidays in P21 & data warehouse, periods) | Side project | Low | Documentation | Keep: Project Focus |
| 2026-07-15 | Fax Number cleanup (ref 20250925) | Side project | Low | — | Keep: Project Focus |
| 2026-07-15 | Portal: order-no on top; click order number → line-location info (see "RE: Report") | Side project | Low | Portal enhancement | Keep: Project Focus |
| 2026-07-15 | P21 Upgrade — add "P21 Web" related docs to folder | Side project | Low | Documentation | Keep: P21 Upgrade Q2 |
| 2026-07-15 | Look at `DirectShipPriceCheck_0801_0805.dll` — item with the same… | Side project | Low | BR investigation | Keep: BR list |
| 2026-07-15 | Fix ProPerks Title in Dev | Side project | Low | — | Keep: BR list |
| 2026-07-15 | Check "validate orders" BR (ref 20251215) | Request | Low | May overlap `kb_Order_Validator_v2` (Active) — verify before starting | Keep: BR list |
| 2026-07-15 | Test Visual Rule — on hold / long-term | Side project | Low | See `project_p21_visual_rules_guide.md` | Keep: 3 Goals |
| 2026-07-15 | Cleanup BR documentation in general | Side project | Low | Overlaps Goal 2 (BR docs, due 2026-10-30) | Keep: BR list |
| 2026-07-15 | Use API to create/update P21 users | Side project | Med | Adjacent to Active "P21 Play API — create orders" (that one is orders, this is users) | Keep: Long Term Goals |
| 2026-07-15 | Update "Add & Remove P21 Users" script to copy messages to clipboard | Side project | Low | PowerShell enhancement | Keep: Long Term Goals |
| 2026-07-15 | Write script to monitor files added to the old file server | Side project | Low | PowerShell | Keep: Long Term Goals |
| 2026-07-15 | Extract data from the parameters column of the execution log/plan | Side project | Low | Investigation | Keep: Long Term Goals |
| 2026-07-15 | Pass SQL SP parameters from VBA (currently not working) | Side project | Low | Investigation | Keep: Long Term Goals |
| 2026-07-15 | `Get-CallerLineNumber` → output to file | Side project | Low | PowerShell | Keep: Long Term Goals |
| 2026-07-15 | Understand "Sale Reps factor of 2" | Nice-to-have | Low | Likely the split-commission ×2 row-doubling (`primary_salesrep`) — see `feedback_p21_split_commission_dedup` | Keep: Long Term Goals |
| 2026-07-15 | Understand EDI 832 (how it works) | Nice-to-have | Low | Learning | Keep: Long Term Goals |
| 2026-07-15 | Improve Power Query skills | Nice-to-have | Low | Learning | Keep: Long Term Goals |
| 2026-07-15 | Focused time on API/Postman (learning) | Nice-to-have | Low | Skill-building companion to the API work above | Keep: Long Term Goals |
| 2026-08-10 | Build Phase 7 — automated `business_rule` capture/restore in the BRR refresh scripts | Side project | Med | Prevents repeating the 8/10 manual recovery on the next P21BusinessRules refresh | See `project_p21businessrules_refresh.md` |
| 2026-08-10 | Fix `$rows.Count` crash in After-P21BusinessRules-Refresh.ps1's location-name verification step | Nice-to-have | Low | `Invoke-Sqlcmd` returns a bare object (no `.Count`) on exactly 1 row; wrap in `@()` | Found 2026-08-10, not yet applied |
