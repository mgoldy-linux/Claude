# Deploy-Guides

Deployment guides for deployable artifacts (views, business rules, portals/.srd, reports, scripts).

**Practice:** every task that produces a deployable artifact gets its **own** guide here, written *during development*. One markdown per task/ticket — not one master file. Start from [`_TEMPLATE.md`](_TEMPLATE.md).

Each guide covers: artifact(s) & ticket · target environments · dependency & deploy order · backward-compat notes · deploy steps · verification · rollback.

## Guides

| Ticket | Title | Status |
|--------|-------|--------|
| SA-46321 | [PO Required Date (Line + Header)](SA-46321-po-required-date.md) | 🟡 **Awaiting user acceptance testing.** Line Required Date = order date + Published Lead Time (fallback Average), slid past weekends/`service_calendar` holidays; header = MAX of line dates, frozen once. Redesigned mid-build after finding P21 auto-populates both fields to order_date on creation (never NULL) — added two marker tables to track "already computed" state instead of an `IS NULL` guard. Also found/fixed a missing `EXECUTE` grant (`p21_application_role`/`PxxiUser`) that failed silently. Full live test matrix passed on 5 real Play POs, committed `52174df`. UAT request sent via SysAid to Mike/Chad/Pamela/Jere. Next: await UAT feedback, then deploy to Prod |
| *(none)* | [Order Acknowledgment Custom Email Message](order-ack-custom-email-message.md) | 🟡 **Awaiting Matt Munson's reply + a decision on `t2`.** 2026-08-10: the P21BusinessRules refresh deleted all 3 registered rules (`business_rule` isn't captured by the refresh scripts — known gap). Recovered `asi_email_context_flag` + `asi_oe_email_close_diag`, re-verified via SQL, re-proven with a real delivered test email. `asi_oe_email_close_diag` came back Synchronous instead of the previously-proven Asynchronous — worked in testing, not fully explained. `t2` (FormPreEmail pre-fill fallback, still injects placeholder joke text) was **not** restored — open call whether to bring it back. ⚠️ t1 caused a real-customer email incident during testing (4 customers, see guide) — apologies sent, closed. Next: decide on `t2`; once Matt replies, build the real rule with his text and deploy to the DB he specifies |
| SSRS Perf: Five Way *(SA-50249 closed, access-only)* | [Five Way Report Acceleration (ASI_ReportCache columnstore)](SA-50249-five-way-report-acceleration.md) | Report 1 under the **SSRS Reports Performance Improvements** initiative (`C:\_P25\SSRS-Report-Performance-Progress.md`). Built on `asdwdb01` (149s→125ms). 2026-07-27: NULL-join bug fixed, fact rebuild armed for 07-20 23:00. **GO-LIVE pending BuildLog 0/0** — nothing live swapped yet |
| SA-48124 | [Vendor Invoice Report — additional EDI charges (subreport)](SA-48124-vendor-invoice-report-charges.md) | 🟡 **Pending A/P validation in Play.** Custom view `asi_view_vendor_invoice_edi_charges` (+`invoice_amount`) live in **Play & Prod** w/ grants. Crystal subreport in `P21VendorInvoiceReport.rpt` **Play only** — itemizes charges + corrected total, suppresses when none. Verified 26172056 → 41.56 / total 597.16. Next: A/P sign-off → `.rpt` to Prod → check 26190433 → 1.05 / 76.53 |
| SA-49504 | [asi_view_edi_855_po_ack (EDI PO-Ack Exceptions portal)](SA-49504-edi-855-view.md) | Deployed to Prod 2026-07-13 — awaiting user feedback |
| SA-47981 | [WWMS Sales Order Picking: Item Description](SA-47981-wwms-pick-item-desc.md) | 🟡 **On hold — display WORKS (2026-07-21).** Converter renders `ufc_p21soc_wwmsitemdesc` on screen (verified CRA197 → "Crain 197 Comfort Knees Knee Pads"). Earlier "unreachable/needs Epicor" verdict retracted — blocker was rule timing, fixed by the bin-selection re-fire. One line-advance test remains to choose ship-as-is vs add-a-button. Non-technical ticket update in `SA-47981/ticket-update.md` |
| SA-48732 | [CSR Open Order Team portal](SA-48732-csr-open-order-team-portal.md) | 🟢 **Closed 2026-07-29** — deployed to Prod 2026-07-27, verified populating, no issues reported |
| SA-51376 | [RMA Transaction History: Reason Code Column](SA-51376-rma-transaction-history-reason-code.md) | 🟢 **CLOSED 2026-08-06.** Reason Code column live on all 21 Prod role versions of Transaction Master Inquiry Orders, RMA tab — built once via Field Chooser New Field, placed on the other 20, verified via SQL. Supplemental index (`ix_lost_sales_transaction_transaction_no`) live in Play and Prod, measured 2,905→3 and 2,951→3 logical reads (scan→seek). Caught and fixed 2 roles left with the join-key column visible instead of hidden |
| *(none)* | [Remove ds_view_open_rma_value (legacy view cleanup)](ds_view_open_rma_value-retirement.md) | 🟢 **Prod done 2026-08-06** — not SA-51376, spun off from it. Non-P21 wrapper around native `p21_view_open_rma_report`, proven equivalent (`EXCEPT`). All 6 affected roles (158/922/923/929/930/2990) in Prod: old field removed, shared definition retargeted, rebuilt via Field Chooser, decimal format fixed, changelog logged — each verified via SQL. Training also fully done. **Play intentionally left at 2 of 6** — will resolve on its next refresh-from-Prod rather than manual completion |
| *(none)* | [BusinessRulesDLL Share Cleanup (business_rule_log Initialize noise)](businessrulesdll-share-cleanup.md) | 🔵 **In progress, paused 2026-08-07.** Root cause: 99.99% of `business_rule_log` was client-startup DLL-scan noise, not real rule activity. Shifted to evidence-based rule↔DLL matching (`Get-BusinessRuleDllStatus.ps1`) after confirming `business_rule` has no stored link between `rule_name` and a DLL. `BusinessRules`: 24 (8/5) + 13 more (8/7) archived; caught + fixed a real same-day mistake (`kb_Shipping_IBFSurcharge.dll` bundled an Active rule, restored within hours, no impact); ~17 Inactive rows left for one-by-one review. `Prod`: started 8/7, 9 files moved (`P21.Accounting.MX.dll` + SaleDay family), `DirectShipPriceCheck`/`OrderEntry_SalesRepAddition` batch not yet run. `Dev`/`Play`/`Training`/`Upgrade`: still only the original 8/5 pass, batch SQL delete not yet run, not yet given the 8/7 2nd-pass treatment |
| *(none)* | [asi_proc_copy_p21_user (retiring kb_proc_copy_user)](asi-proc-copy-p21-user.md) | 🟢 **Deployed 2026-08-04** to all 6 P21 environments (Prod included), grants verified. Fixed 3 real bugs (description/job_title not inheriting, Role mislabeled as job_title, blank-vs-NULL on Salesrep/Buyer ID) + added Buyer ID to output. PowerShell wrapper `Add-P21User-v1.ps1` fixed and verified. `kb_proc_copy_user` not yet retired — coexists |
| *(none)* | [asi_proc_compare_p21_users (P21 user diff tool)](asi-proc-compare-p21-users.md) | 🟢 **Built + verified 2026-08-06**, deployed to Prod + P21Play only (other 4 envs on demand). Read-only, diffs `users`/`users_ud`/security/portals/company between two users. Built for SysAid 51844 (Rovello access mirroring) — used to compare EROVELLO vs RWAYMAN/GGIERUM in Prod. Trap found: P21Play showed a stale `delete_flag` diff from refresh lag — real access comparisons must run against Prod |
| *(none — recurring)* | [fcB2B Quarterly 810/850 Metrics Report (WFCA)](fcb2b-quarterly-810-850-report.md) | 🟡 **Recurring, not a one-time deploy.** Rebuilt `Create-B2BMetricsReport.ps1` (added `-FiscalYear`/`-Quarter` params, merged the 810-side queries: 62,669 → 1,454 logical reads measured). FYQ2 and FYQ3 2026 generated, verified, and sent to Lewis Davis (WFCA). Found & documented 2 historical data-quality issues (FYQ4 2025 mislabeled year, FYQ3 2025 cumulative-looking values) — recomputed FYQ3 2025 for the record, both **closed 2026-07-31** (not worth further effort). **Next: FYQ4 2026, due ~2026-10-12** |
| *(none)* | [Low Margin Alert (Evan Jenkins)](low-margin-alert.md) | Main pair deployed to Prod INACTIVE (705) 2026-07-31; awaiting Evan's OK on 7/22 spacing before flip to 704. PAD pair deployed to Prod INACTIVE 2026-08-03 but **missed that same day's Play refresh backup window** — rebuilding directly in Play, and the SQL-built rebuild won't fire (likely AHI-API1 token cache), so now rebuilding manually in the P21 client. **Not confirmed firing** |
| *(none)* | [90 Day Usage Report — blank subscriptions (Jami Klitzman)](ssrs-90-day-usage-report-blank-subscriptions.md) | 🔴 **Forced-rerun mechanism blocked; durable fix unaffected.** Durable `mfg` fix still confirmed applied 2026-07-27 (all 44 locations). Jami's one-time forced rerun on 2026-07-28 evening did **not** work — fixed a real `FireEvent` 3-arg signature bug, but even after that, events queue/dequeue yet nothing is ever delivered (verified via `ExecutionLog3` + a live single-subscription test). Root cause needs SSRS trace-log access on `ASDWDB01`, not available this session. Real validation now rides on the normal Sunday 8/2 run. 5 new branches (Naperville, St. Cloud, Omaha, Wixom, Ft Wayne) still unchecked either way |
